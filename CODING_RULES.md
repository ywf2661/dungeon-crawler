# CODING_RULES.md

## 1. 기본 원칙

1. 사용자의 요청 범위를 지킨다.
2. 기존 기능을 임의로 변경하지 않는다.
3. 요청하지 않은 리팩토링을 하지 않는다.
4. 새로운 라이브러리/프레임워크를 임의로 추가하지 않는다.
5. 기존 프로젝트 구조와 파일 로드 순서를 유지한다.
6. 실제 코드와 문서가 다르면 실제 코드를 우선한다.
7. 작업에 필요한 최소한의 파일만 확인한다.
8. 필요한 최소한의 코드만 수정한다.

---

## 2. 소스코드 접근 규칙

### 가장 중요한 규칙

**프로젝트에 이미 존재하는 실제 소스코드를 먼저 확인한다.**

사용자가 현재 대화에서 파일을 첨부하지 않았다는 이유만으로 파일을 요청하지 않는다.

다음 순서로 확인한다.

```text
1. 프로젝트에 존재하는 실제 소스
2. 현재 대화에 첨부된 소스
3. 프로젝트 문서
4. 필요한 코드가 실제로 존재하지 않을 때만 사용자에게 요청
```

### 금지

다음과 같은 요청을 불필요하게 하지 않는다.

```text
"state.js를 올려주세요."
"storage.js도 올려주세요."
"관련 파일을 전부 올려주세요."
```

해당 파일이 프로젝트에 이미 존재한다면 먼저 직접 확인한다.

### 파일 요청이 필요한 경우

프로젝트에서 필요한 파일을 찾을 수 없거나,
실제 코드 내용에 접근할 수 없는 경우에만 파일명을 요청한다.

예:

```text
현재 작업에 필요한 실제 코드가 프로젝트에서 확인되지 않습니다.

필요한 파일:
- js/combat/battle-end.js
- js/storage.js
```

---

## 3. 토큰 절약 규칙

Claude는 매 작업마다 전체 프로젝트를 다시 분석하지 않는다.

### 하지 않는 것

* 전체 소스코드 재분석
* 관련 없는 파일 확인
* 이미 확인한 파일 반복 확인
* 변경하지 않은 코드 전체 출력
* 전체 파일을 불필요하게 다시 출력
* 이미 문서에 있는 내용을 장황하게 재설명
* 동일한 파일을 반복해서 사용자에게 요청

### 해야 하는 것

```text
작업 요청
→ 관련 파일 식별
→ 필요한 파일만 확인
→ 최소 범위 수정
→ 변경 내용만 출력
```

---

## 4. 작업 규모별 처리

### 작은 작업

예:

* 수치 변경
* 텍스트 변경
* 아이템 설명 변경
* 확률 변경
* 단순 UI 수정

→ 바로 구현한다.

---

### 중간 작업

예:

* 신규 스킬
* 신규 몬스터
* 신규 이벤트
* 신규 아이템
* 기존 시스템에 작은 기능 추가

→ 영향 파일을 짧게 확인한 후 구현한다.

---

### 큰 작업

예:

* 저장 구조 변경
* 체크포인트 시스템
* 전투 시스템 변경
* 노드맵 구조 변경
* 새로운 핵심 시스템 추가

→ 다음 순서로 진행한다.

```text
1. 현재 구조 확인
2. 영향 파일 확인
3. 변경 계획 제시
4. 위험 요소 설명
5. 사용자 승인
6. 구현
```

---

## 5. 코드 수정 규칙

* 기존 함수명을 가능한 한 유지한다.
* 기존 변수명을 가능한 한 유지한다.
* 기존 데이터 구조를 가능한 한 유지한다.
* 기존 호출 흐름을 임의로 변경하지 않는다.
* 기존 저장 데이터와의 호환성을 고려한다.
* 새로운 전역 상태를 최소화한다.
* 변경 범위를 최소화한다.

---

## 6. 게임 시스템 규칙

사용자가 명시하지 않은 게임 디자인 변경을 임의로 하지 않는다.

특히 다음은 사용자 승인 없이 변경하지 않는다.

* 직업
* 스킬
* 장비
* 유물
* 몬스터
* 보스
* 난이도
* 전투 규칙
* 노드맵 규칙
* 저장/체크포인트 규칙
* 보상 구조

아이디어가 떠오른 경우:

```text
구현하지 않고 제안만 한다.
```

---

## 7. 전투 코드 규칙

전투 시스템은 파일 간 의존성이 높으므로 변경 전에 실제 호출 관계를 확인한다.

특히:

* `checkBattleEnd()`
* `enemyTurn()`
* `enemyTurnReal()`
* `enemyAction()`
* `playerAttack()`
* `playerSkill()`
* `playerItem()`

의 호출 흐름을 임의로 변경하지 않는다.

전투 종료, 턴 처리, 상태이상, 유물 효과의 순서를 변경할 경우 반드시 영향 범위를 확인한다.

---

## 8. 저장/체크포인트 규칙

저장 시스템과 체크포인트 시스템을 구분한다.

### 일반 저장

게임을 종료하거나 이어하기 위한 영속 데이터.

### 체크포인트

던전 구간에서 사망했을 때 복원하기 위한 게임 상태.

체크포인트 기능을 추가할 경우 다음을 별도로 검토한다.

* 체크포인트 생성 시점
* 체크포인트 데이터
* 복원 시점
* 일반 저장 데이터와의 관계
* 플레이어 상태
* 골드
* 경험치
* 레벨
* 장비
* 유물
* 포션
* 노드맵 진행 상태

---

## 9. UI 규칙

* 기존 DOM id를 임의로 변경하지 않는다.
* 기존 CSS class를 임의로 변경하지 않는다.
* 새 UI는 기존 UI 구조에 맞춰 추가한다.
* 이벤트 바인딩은 기존 구조를 따른다.
* UI 변경을 이유로 관련 없는 화면을 재작성하지 않는다.

---

## 10. 검증 규칙

가능하면 다음을 확인한다.

```text
문법 오류
참조 오류
함수 호출 관계
데이터 id 오류
저장/복원 필드 누락
```

검증하지 못한 부분은 명확하게 표시한다.

검증하지 않은 것을 정상 동작한다고 단정하지 않는다.

---

## 11. 코드 출력 규칙

기본적으로 **변경된 코드만 출력한다.**

전체 파일 출력은 다음 경우에만 한다.

* 파일 대부분을 변경한 경우
* 부분 코드로 적용하기 어려운 경우
* 사용자가 전체 파일을 요청한 경우

기본 출력:

```text
파일:
변경 위치:
변경 내용:
변경 코드:
```

---

## 12. 작업 완료 규칙

작업 완료 후 다음만 간단하게 정리한다.

```text
변경 파일
변경 내용
검증 결과
미검증 항목
```

그리고 현재 구현 상태가 변경되었다면 `CURRENT_STATUS.md`에 반영할 내용을 함께 제시한다.

---

## 13. CURRENT_STATUS 관리

`CURRENT_STATUS.md`는 현재 상태를 빠르게 파악하기 위한 문서다.

길게 만들지 않는다.

다음 정보만 유지한다.

* 현재 구현 완료 상태
* 최근 변경사항
* 현재 작업
* 미검증 사항
* 다음 작업

과거의 상세한 작업 과정은 기록하지 않는다.

---

## 14. 작업 중단 기준

다음 상황에서는 임의로 구현하지 않고 사용자에게 확인한다.

* 기존 구조와 새로운 요구사항이 충돌하는 경우
* 데이터 손실 가능성이 있는 경우
* 저장 데이터 호환성이 깨질 수 있는 경우
* 여러 시스템의 설계를 동시에 변경해야 하는 경우
* 요구사항이 모호해서 결과가 크게 달라질 수 있는 경우

---

## 15. 최우선 원칙

> **프로젝트에 존재하는 실제 코드를 먼저 확인한다.**

> **필요한 파일만 읽는다.**

> **필요한 코드만 수정한다.**

> **변경된 내용만 출력한다.**

> **기존 기능을 최대한 보존한다.**

> **모르는 부분은 추측하지 않는다.**

> **사용자에게 같은 소스 파일을 반복해서 요구하지 않는다.**
