# HANDOFF

## 현재 작업

이전 세션에서 완료된 22개 파일 분리 리팩토링 위에서, 이번 세션은 **기능 수정 4건**을
진행했다. 파일 구조 변경은 없었고, 기존에 분리된 파일 중 4개(`player.js`, `relics.js`,
`explore.js`, `combat/battle-setup.js`)만 부분 수정했다.

## 이번 세션에서 완료한 작업

### 1. 유물 제단 "고르지 않는다" — 횟수 제한 → 골드 소모로 변경
- **파일**: `relics.js`
- 기존: 런당 2회까지만 스킵 가능(`player.relicSkipsUsed`/`relicSkipsMax`)
- 변경: 횟수 제한 없이, **골드 1000을 소모**하면 몇 번이든 스킵 가능
- `RELIC_SKIP_GOLD_COST = 1000` 상수 추가, `showRelicAltar()`의 스킵 버튼 표시/활성화/클릭
  로직을 골드 보유 여부 기준으로 교체
- `player.relicSkipsUsed`/`relicSkipsMax` 필드는 **삭제하지 않고 그대로 둠**(아래
  "알려진 문제" 참고)

### 2. 보통 난이도 유물 슬롯 2개 → 3개
- **파일**: `player.js`
- `newPlayer()`의 `relicSlots` 계산식 변경: 쉬움 2 / **보통 3(신규)** / 하드코어 4
  ```js
  relicSlots: diff==='hardcore'?4:(diff==='normal'?3:2)
  ```

### 3. '나아가다' 버튼 연타 시 2층씩 진행되는 버그 수정
- **파일**: `explore.js`
- 원인: `onAdvance()` → `proceedAdvance()`에서 `depth += 1`이 동기적으로 즉시 일어나지만,
  이어지는 전투/제단 진입은 `setTimeout(350~500ms)`으로 지연 실행됨. 그 사이 버튼이 계속
  클릭 가능한 상태로 남아, 짧은 시간 안에 두 번 누르면 `onAdvance()`가 두 번 실행되어
  깊이가 2 증가
- 수정: `onAdvance()` 진입 시 500ms 잠금 적용. JS 변수 잠금(`exploreAdvanceLock`) +
  버튼 DOM `disabled` 처리를 이중으로 적용해, 브라우저 차원에서도 재클릭이 씹히도록 함
- 사용자 확인 결과 **정상 동작 확인됨**
- 단, 최종보스 확인 오버레이의 "들어간다"(`final-confirm-go`) 버튼은 `onAdvance()`를
  거치지 않고 `proceedAdvance()`를 직접 호출하는 별도 진입점이라 이번 수정 범위에서 제외됨
  (동일한 연타 취약점이 이론상 남아있을 수 있음, 아래 "알려진 문제" 참고)

### 4. 유물 밸런스 조정 + 신규 UI
- **파일**: `relics.js`, `combat/battle-setup.js`
- **무한한 탄창**(`relic_infiniteclip`): 스킬 사용 시 MP 무소모 확률 10% → **50%**
- **불확실성의 주사위**(`relic_dice`) 발동 결과 UI 추가: 전투 시작 시 어떤 효과
  (공격력/마력/방어력/최대HP +30%, 속도 +10, 받는 피해 +30% 중 하나)가 뽑혔는지
  `showToast()`로 표시. 기존에 미사용 상태였던 `relics.js`의 `DICE_EFFECT_LABELS`를
  그대로 재사용(신규 데이터 추가 없음). `battle-setup.js`의 `startBattle()` 끝부분,
  화면 전환·연출이 끝난 직후에 호출되도록 배치

## 변경된 파일

이번 세션에서 아래 4개 파일만 수정했다(모두 기존 22개 분리 파일 중 일부, 새 파일
생성 없음).

| 파일 | 변경 내용 |
|---|---|
| `player.js` | `relicSlots` 계산식(보통 난이도 3개) |
| `relics.js` | 유물 스킵 골드화, 무한한 탄창 확률 상향, DICE_EFFECT_LABELS export 주석 정리 |
| `explore.js` | `onAdvance()` 재진입 방지 잠금(`exploreAdvanceLock` + 버튼 disabled) |
| `combat/battle-setup.js` | `startBattle()`에 주사위 효과 토스트 표시 추가, 의존성 주석 갱신 |

**주의**: 이 4개 파일은 대화 중 개별 다운로드 형태로 사용자에게 전달되었을 뿐,
`ywf2661/dungeon-crawler` 리포지토리나 실제 배포본에 반영되었는지는 이번 세션에서
확인되지 않았다. 다음 세션 시작 시 실제 프로젝트 폴더의 `js/player.js`,
`js/relics.js`, `js/explore.js`, `js/combat/battle-setup.js`가 이 문서에 기술된
내용과 일치하는지 먼저 확인이 필요하다.

## 현재 정상 동작

- 4개 파일 모두 `node --check` 문법 통과 확인
- '나아가다' 연타 버그: 사용자가 직접 재현 테스트하여 수정 확인함("적용 잘됐어" 확인)
- 나머지 3건(유물 스킵 골드화/보통 난이도 슬롯 3개/무한한 탄창 확률/주사위 토스트)은
  코드 수정 및 문법 검증만 완료된 상태 — **사용자의 브라우저 실동작 확인은 아직
  이루어지지 않음**

## 테스트 필요

- 유물 제단에서 "고르지 않는다" 클릭 시 골드 1000 정상 차감 및 상태바 갱신 확인
- 골드 1000 미만일 때 스킵 버튼이 정상적으로 비활성화되는지 확인
- 보통 난이도로 캐릭터 생성 시 유물 슬롯이 실제로 3개로 표시되는지 확인
- 무한한 탄창 보유 상태에서 스킬 반복 사용 시 체감 확률이 50%에 가까운지 확인
- 불확실성의 주사위 보유 상태로 전투 시작 시 토스트가 정상 표시되는지, 문구가
  실제 적용된 효과와 일치하는지 확인
- (기존 HANDOFF.md에서 이어지는 미해결 항목) 유물/저주 제단, 장비 세트효과,
  전직 UI, 보스소굴, 최종보스, 사운드, PWA, `window.storage` 저장 등은 이전
  세션부터 누적된 미착수 수동 테스트 항목 — 이번 세션에서도 다루지 않음

## 알려진 문제

- **`player.relicSkipsUsed`/`relicSkipsMax` 필드가 죽은 데이터로 남아있음**:
  스킵 로직이 골드 소모 방식으로 바뀌면서 더 이상 참조되지 않지만, `storage.js`의
  저장/불러오기 로직을 확인하지 않은 상태라 안전하게 필드 자체는 삭제하지 않고
  `player.js`에 그대로 남겨둠. `storage.js`가 특정 필드를 명시적으로 다루는
  방식이라면(필드 목록 나열형), 이 두 필드를 완전히 정리하는 후속 작업이 필요할 수
  있음
- **최종보스 확인 오버레이의 "들어간다" 버튼**: `onAdvance()`의 연타 방지 잠금을
  거치지 않고 `proceedAdvance()`를 직접 호출함 — 이론상 동일한 연타 취약점이 남아있을
  가능성이 있으나, 사용자가 보고한 버그(일반 "나아가다" 버튼)와는 다른 지점이라
  이번 세션에서는 다루지 않음
- 이 외 기존 HANDOFF.md에서 언급된 문제는 없음(이번 세션 범위 밖)

## 주의사항

- (기존과 동일) `index.html`의 `<script src>` 순서 임의 변경 금지
- (기존과 동일) `enemyTurn` 4단 래핑 순서, `rogueRegisterHit`/`consumeOnHitBonuses` 등
  소비형 로직 호출 횟수/순서 유지
- 이번 세션에서 수정한 4개 파일을 실제 프로젝트에 반영할 때는, 파일 전체를
  교체하지 말고 diff 기준으로 반영하는 것을 권장(각 파일에 이전 세션 이후 다른 경로로
  추가 수정이 있었을 가능성 배제 불가)

## 현재 개발 상태

리팩토링 이후 게임 기능 구현 상태는 기존 PROJECT.md 13번 항목과 동일하며, 이번
세션에서 진행한 기능 수정만 별도로 정리하면:

| 작업 | 상태 |
|---|---|
| 유물 스킵: 횟수 제한 → 골드 1000 소모 | 완료(코드), 실동작 미확인 |
| 보통 난이도 유물 슬롯 2→3개 | 완료(코드), 실동작 미확인 |
| '나아가다' 연타 시 2층 진행 버그 수정 | 완료, **사용자 확인 완료** |
| 무한한 탄창 확률 10%→50% | 완료(코드), 실동작 미확인 |
| 불확실성의 주사위 발동 토스트 UI | 완료(코드), 실동작 미확인 |
| `player.relicSkipsUsed`/`relicSkipsMax` 죽은 필드 정리 | 미착수(storage.js 확인 필요) |
| CSS 파일 분리(`styles.css`) | 미착수(이전 세션부터 이어짐) |
| UI/게임 로직 분리 | 미착수(이전 세션부터 이어짐) |
| GitHub 리포지토리(`ywf2661/dungeon-crawler`)에 반영 | 미착수 |
| 브라우저 수동 테스트(이전 세션 "테스트 필요" 목록 전체) | 미착수 |

## 다음 작업

1. 이번 세션에서 수정한 4개 파일(`player.js`, `relics.js`, `explore.js`,
   `combat/battle-setup.js`)을 실제 프로젝트 폴더에 반영
2. 위 "테스트 필요" 항목을 브라우저에서 직접 확인
3. (검토) `player.relicSkipsUsed`/`relicSkipsMax` 필드 완전 삭제 여부 —
   `storage.js`의 저장/불러오기 방식을 먼저 확인해야 함
4. (검토) 최종보스 확인창의 "들어간다" 버튼에도 동일한 연타 방지 로직을 적용할지 여부
5. 이전 세션부터 이어지는 미착수 항목(CSS 분리, GitHub 반영, 전체 수동 테스트)은
   여전히 대기 중 — 우선순위는 사용자 확인 필요

## 다음 작업 시 확인할 파일

- `player.js`, `relics.js`, `explore.js`, `combat/battle-setup.js` — 이번 세션
  변경분이 실제로 반영되었는지 대조할 때 사용
- `storage.js` — `relicSkipsUsed`/`relicSkipsMax` 필드 정리 여부를 판단하려면
  가장 먼저 확인해야 할 파일(저장 로직이 필드를 개별 나열하는 방식인지 확인)
- `README.md` — 리팩토링 원리(공유 렉시컬 스코프) 설명. 이번 세션 수정 파일들도
  모두 이 원리를 그대로 따름(새 전역 변수 `exploreAdvanceLock`, `RELIC_SKIP_GOLD_COST`
  등은 각자의 파일 최상단에 선언되어 있음)

## 작업 중단 지점

이번 세션은 4건의 기능 수정(유물 스킵 골드화, 보통 난이도 유물 슬롯 3개, '나아가다'
연타 버그 수정, 유물 밸런스+UI 조정)을 마친 상태에서 종료되었다. '나아가다' 버그
수정은 사용자가 직접 재현 테스트해 정상 동작을 확인했으나, 나머지 3건은 코드 수정과
문법 검증만 마친 상태로 아직 브라우저 실동작 확인 전이다. 다음 세션은 "다음 작업"
1번(파일 반영 확인)과 2번(브라우저 테스트)부터 시작하면 된다.
