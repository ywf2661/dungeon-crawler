# HANDOFF

## 현재 작업

전직 시스템 개편(`JOB_SPECIALIZATIONS` 2분기 선택) 이어서 진행. 이전 세션에서
전사·마법사(4개 분기, 8개 스킬)까지 완료된 상태를 업로드받아 검증했고, 이번
세션에 **도적**(환영검사/맹독 연금술사) 2개 분기·6개 스킬(마스터리 2 + 액티브
2, 단 `rogueShadowStrike`는 공용 로직 확장 방식이라 신규 실행 코드는 없음)을
추가로 구현했다. 남은 직업은 성기사/메카닉/도박사 3개.

**중요**: 이전 HANDOFF의 "최우선 확인 사항"이었던 `job-advancement.js`를 이번에
확보해 확인한 결과, `resolveJobAdvancement()`가 이미 `masterySkillId`/
`activeSkillId`를 `player.skills`에 지급하는 로직을 갖추고 있었다. **그 문제는
해결된 상태**이며, 별도 조치가 필요 없다.

## 리팩토링 결과

이번 세션은 구조 변경이 아니라 기존 22파일 구조 안에서 기능(신규 스킬) 추가만
진행했다. 구조/의존관계는 PROJECT.md 그대로다.

## 변경된 파일

- `data/skills.js` — 도적 분기 6개 스킬 추가(`mastery_afterimage`,
  `rogueShadowStrike`, `mastery_triplepoison`, `rogueCatalyst`)
- `combat/enemy-turn.js` — `triggerAfterimageStrike()` 신규 함수 추가,
  `enemyTurn()`에 `battleFlags.afterimagePending` 체크 분기 추가(기존 마녀의
  시계/가동장치 체크와 같은 자리, 같은 타이밍 규칙), `applySkillModifiers()`에
  `s.guaranteedCritMult` 처리 추가(조건 없이 항상 발동하는 확정 배율)
- `combat/player-actions.js` — `playerAttack()`에 잔영 예약(25% 확률)과 삼중
  조제 축적/폭발 로직 추가, `playerSkill()`에 신규 타입 `catalyst` 처리 분기
  추가

(`jobs.js`, `job-advancement.js`는 이전 세션 산출물을 그대로 확인만 하고 이번
세션에서 수정하지 않았다.)

## 현재 정상 동작

jsdom 스모크 테스트로 다음을 확인함(전 직업 공통):

- 전사: 혈서(토글) → 저돌(HP 소모+피해 증폭) 흐름, 인내 스택 축적 → 파훼일격
  소모 흐름 모두 예외 없이 동작
- 마법사: 원소 계약이 걸린 상태에서 마법 스킬 시전 시 정상 처리, 연쇄폭발
  정상 피해, 시간 왜곡 보유 시 확률 체크 정상 합산, 가속 주문이 전투 화면을
  유지한 채(적 턴 스킵) 정상 종료
- **도적(이번 세션 신규 검증)**:
  - 잔영: 기본 공격 적중 시 `battleFlags.afterimagePending`이 정상 세팅되고,
    적 턴이 열릴 때(`enemyTurn()`) 예약된 분신 공격이 약 450ms 뒤 정확히 1회
    발동해 피해를 입힌 뒤 정상적으로 적의 턴으로 이어짐(전투 미종료 확인)
  - 그림자일격: `guaranteedCritMult` 배율이 정상 적용되어 피해 발생, 크리티컬
    연출(`mod.triggered`)도 함께 발동
  - 삼중 조제: 기본 공격마다 독 3종 중 하나가 정상적으로 축적되고, 3종이 모두
    채워진 뒤 다시 공격하면 폭발 피해 발생 + 축적 초기화까지 확인
  - 촉매 주입: 부족한 독 슬롯을 정상적으로 채움. (단, MP 부족 시 기존
    `playerSkill()`의 MP 체크에 의해 조용히 무시됨 — 신규 로직 문제 아니라
    기존 게임의 표준 동작)
- 전체 22개 파일 `node --check` 문법 통과, jsdom 기반 실제 `<script src>`
  로딩 재현 테스트에서 `WINDOW ERROR` 없음

## 테스트 필요

- 이전 HANDOFF에서 넘어온 항목 그대로 유효: 유물/저주 제단, 장비 세트효과,
  보스소굴, 최종보스+광폭화, 실제 브라우저에서의 사운드 재생, PWA 설치,
  `window.storage` 경로(아티팩트 환경) — 전부 여전히 브라우저 수동 테스트
  안 됨(스모크 테스트는 jsdom 환경, Web Audio는 스텁 처리)
- **잔영/삼중 조제가 스킬로 인한 적중에는 반응하지 않고 기본 공격에만
  반응하는 것이 실제 플레이 밸런스상 허전하게 느껴지는지** — 설계상 의도적
  범위 제한이었으나(아래 "알려진 문제" 참고), 실제 플레이 후 재검토 필요
- 잔영 발동 확률(25%), 그림자일격 배율(1.7배), 삼중 조제 폭발 배율(1.4배)은
  전부 다른 유사 스킬과 감(느낌)으로 맞춘 가배치 수치 — 밸런스 검증 안 됨

## 알려진 문제 / 미해결

- **잔영(mastery_afterimage)·삼중 조제(mastery_triplepoison)는 기본 공격에만
  반응하고, 도적 자신의 스킬(연속 베기/급소 찌르기 등)로 적중해도 반응하지
  않는다.** 마법사 계약술사의 "매 턴 시작" → "스킬 시전 시" 단순화와 같은
  종류의 **의도된 설계 타협**이다(범위를 좁혀 리스크를 줄임). 도적 스킬 대부분이
  `multihit`/`drain` 타입이라 실제로는 기본 공격보다 스킬을 더 자주 쓰게 되므로,
  나중에 사용자가 "스킬 적중에도 반응하게 확장" 여부를 재검토할 수 있음
- **촉매 주입(rogueCatalyst)의 "원하는 독 하나"가 실제로는 무작위 하나**로
  단순화되어 있다(선택 UI가 없어서). 마법사 계약술사와 동일한 종류의 타협
- `job-advancement.js`는 확보했지만, **레벨10 도달 시 실제로 전직 UI가 뜨는
  트리거 지점**(어느 파일이 `player.jobAdvancePending`을 언제 세우고
  `showJobAdvancement()`를 언제 호출하는지)은 여전히 확인하지 않았다. 이번
  세션은 `resolveJobAdvancement()`를 테스트에서 직접 호출해 지급 로직만
  검증했을 뿐, 실제 레벨업 → UI 팝업 흐름 전체는 미검증
- 저주 제단 보상 강화 방향 미확정(이전 세션부터 계속 대기 중)
- CSS 분리, GitHub 리포지토리(`ywf2661/dungeon-crawler`) 반영, 전체 브라우저
  수동 테스트 — 전부 여전히 미착수

## 주의사항

- **`enemyTurn()`의 체크 순서가 이번 세션에 하나 더 늘었다**: 마녀의 시계/
  시간 왜곡 확률 체크 → **잔영(afterimagePending) 체크(신규)** → 가동 장치
  (rig) 체크 → `enemyTurnReal()`. 다음 직업 구현 때 "적 턴이 열리기 직전
  자동 발동" 계열 효과를 또 추가하게 되면 이 순서 안에 끼워 넣을 것 —
  순서를 바꾸면 여러 자동발동 효과가 같은 턴에 겹칠 때 우선순위가
  달라진다
- `battleFlags.afterimagePending`, `battleFlags.triplePoison`은 이번 세션에
  신규 도입한 필드다. `battleFlags` 자체가 전투마다 새로 생성되는지는
  `combat/battle-setup.js`에서 확인해야 하는데(여전히 미확보), 지금까지
  두 세션 모두 이 파일 없이도 새 필드가 문제없이 동작하는 것으로 보아
  `battleFlags`는 매 전투 `{}`로 새로 생성되는 것으로 추정된다(확실하진
  않음 — 파일을 확보하면 확정할 것)
- `mastery_afterimage`/`mastery_triplepoison`의 축적/판정 로직은 전부
  `playerAttack()` 안에 직접 인라인되어 있다(전사/마법사 세션의 관례를 그대로
  따름 — 별도 함수로 추출하지 않음). 다음 직업(성기사/메카닉/도박사) 중에도
  "기본 공격 적중 시" 계열 마스터리가 있다면 같은 자리에 이어서 추가하면 됨
- 새 스킬 타입(`catalyst`)도 기존 관례대로 `playerSkill()` 안의 `s.type==='guard'`
  분기보다 앞쪽에 추가했다

## 다음 작업

1. **최우선**: 성기사 분기(순교자/계율의 파수꾼) 구현 — `jobs.js`의
   `JOB_SPECIALIZATIONS.paladin`에 이미 이름/설명/masterySkillId/activeSkillId가
   정의되어 있으므로, 전사·마법사·도적과 동일한 패턴으로 `data/skills.js` +
   `combat/player-actions.js` + `combat/enemy-turn.js`에 이어서 구현
2. 이어서 메카닉(로봇군단장/데토네이터), 도박사(운명의 반란자/패의 마술사) 순서로 진행
3. 6개 직업 전부 완료되면, 레벨10 도달 시 실제 전직 UI가 뜨는 트리거 지점을
   확보해 "테스트 필요"에 남은 실제 게임 플로우(레벨업 → 팝업 → 선택 →
   지급)를 처음부터 끝까지 검증
4. 잔영/삼중 조제를 스킬 적중에도 반응하도록 확장할지 여부 사용자 확인 후 결정
5. 촉매 주입의 "무작위" 단순화를 실제 선택 UI로 개선할지 여부 검토
6. 이전부터 밀려있는 항목(저주 제단 보상, CSS 분리, GitHub 반영, 전체 브라우저
   테스트) 우선순위 재확인

## 다음 작업 시 확인할 파일

- `data/jobs.js`의 `JOB_SPECIALIZATIONS.paladin` / `.mechanic` / `.jester` —
  다음 3개 직업의 이름/설명/masterySkillId/activeSkillId가 이미 정의되어
  있음(재요청 불필요, 이번 대화에 이미 전체 내용 확보됨)
- `data/skills.js`, `combat/player-actions.js`, `combat/enemy-turn.js` — 다음
  직업 구현 시 이번과 동일한 패턴으로 계속 열어야 하는 3개 파일
- `combat/battle-setup.js` — 여전히 미확보. `battleFlags` 초기화 시점을
  확정하려면 필요(위 "주의사항" 참고)
- 플레이어 턴이 실제로 다시 열리는 지점을 정의한 파일(파일명 미상) — 계약술사의
  "턴 시작" 훅 정확화, 향후 다른 "매 턴 자동 발동" 계열 마스터리 구현 시 필요할
  수 있음

## 작업 중단 지점

전사(2분기) + 마법사(2분기) + 도적(2분기), 총 6개 분기·14개 스킬 구현과 검증을
마친 상태에서 중단됨. 성기사/메카닉/도박사 3개 직업(6개 분기)이 아직 남아있고,
그 중 어느 것도 시작하지 않았다. 다음 세션은 "다음 작업" 1번(성기사 분기 구현)
부터 이어가면 된다. 사용자로부터 다음 지시가 아직 없는 상태.
