## AI 개발 작업 시 소스 접근 원칙

이 프로젝트는 여러 Claude 대화/계정을 이용해 지속적으로 개발한다.

Claude는 작업을 시작할 때 다음 우선순위를 따른다.

```text
1. Claude Project에 존재하는 실제 소스코드
2. 현재 대화에 첨부된 소스코드
3. PROJECT.md / ARCHITECTURE.md / CURRENT_STATUS.md
4. 필요한 정보가 존재하지 않을 경우 사용자에게 파일 요청
```

### 중요한 원칙

* 프로젝트에 이미 존재하는 소스코드는 사용자가 현재 대화에서 다시 첨부하지 않아도 먼저 확인한다.
* 동일한 파일을 반복해서 요청하지 않는다.
* 전체 프로젝트를 다시 업로드하도록 요구하지 않는다.
* 작업과 직접 관련된 파일만 읽는다.
* 실제 코드와 문서가 다르면 실제 코드를 source of truth로 취급한다.
* 필요한 코드가 프로젝트에 존재하지 않는 경우에만 사용자에게 파일명을 요청한다.

### 작업 방식

```text
작업 요청
 ↓
PROJECT.md 확인
 ↓
CURRENT_STATUS.md 확인
 ↓
관련 파일 식별
 ↓
프로젝트에 실제 파일이 있는지 확인
 ↓
있으면 직접 확인
 ↓
없으면 사용자에게 파일 요청
 ↓
작업 규모 판단
 ↓
단순 작업 → 바로 구현
중간 작업 → 짧은 영향 분석 후 구현
대규모 작업 → 계획 제시 → 승인 후 구현
```

### 토큰 절약

Claude는 다음을 피한다.

* 전체 프로젝트 재분석
* 이미 확인한 파일의 반복 분석
* 요청하지 않은 전체 파일 출력
* 변경하지 않은 코드 재출력
* 동일한 파일 재요청
* 이미 문서에 기록된 내용을 다시 장황하게 설명
