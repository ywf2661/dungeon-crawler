# CURRENT_STATUS.md — 현재 개발 상태

> 이 문서는 과거 대화의 대체물이다.
> 새 Claude 대화를 시작할 때 현재 상태를 빠르게 전달하기 위한 문서다.
> 오래된 상세 작업 이력은 `HANDOFF.md`를 참고한다.

## 기준 브랜치

- `main` (이번 세션도 `main`/`mechanic-stoker-accumulator` 양쪽에 동일 반영)

## 현재 확인된 상태

- 프로젝트는 Vanilla JS/HTML/CSS 기반의 정적 브라우저 게임이다.
- 여러 classic `<script>`가 로드 순서에 따라 공유 전역 scope를 사용하는 구조다.
- 전투/탐험/직업/장비/유물/저장 등 기능이 여러 JS 파일로 분리되어 있다.
- `js/`에는 기존 문서에 적힌 파일 외에도 `events.js`, `nodemap.js`, `origin.js`가 존재한다.
- 기존 `PROJECT.md`는 일부 최신 파일/구조보다 뒤처져 있으므로 실제 코드를 우선한다.

## 현재 상태 요약

이번 세션은 **세계관 스토리 통합** 작업이었다. 조사 결과, "왕이 마녀와
계약해 왕국이 회랑에 갇혔다"는 줄기 서사에 필요한 뼈대(회랑의 시조=왕,
잠식된 OO 용사=파수꾼이 된 침입자, 칼리버 X=아코스의 의지, 마녀의 시계
유물, 문지기 캐릭터, "회랑의 ○○" 몬스터 명명 규칙)가 이미 대부분 코드에
존재하고 있었다. 이번 세션은 그 위에 텍스트/대사 레이어를 얹는 작업 위주로
진행했고, 전투 로직 체인(`enemyTurn()`/`checkBattleEnd()`)은 전혀 건드리지
않았다. 이전 세션(독사 밸런스 조정)의 상세 내역은 `HANDOFF.md`로 이동했다.

## 최근 작업 (스토리 통합, 4개 커밋)

1. **저위험 간접 서사 텍스트** (커밋 `72156b8`)
   - `data/equipment.js`: 칼리버 X 1/2/3단계 desc에 "Achos"라는 이름이
     새겨짐→검이 되뇜→완전히 지워짐 흐름 추가
   - `data/monsters.js`: LOCATIONS(망자의 묘실/회랑의 심장부) 지역 묘사에
     문장(紋章)/멈춘 시간 단서 추가
   - `origin.js`: 오프닝 마지막에 문지기의 회중시계 복선 대사 추가
   - `combat/battle-end.js`: 진엔딩 첫 문장에 왕관/슬픈 얼굴 묘사 추가
2. **물음표 이벤트 25개(+조건부 재회 1개) 플레이버 텍스트 교체** (커밋 `a0e84dd`)
   - `events.js` 전체 — 버튼 id/조건문/수치는 전혀 안 건드리고 도입 문단만
     교체. "악마" 계열 2개는 마녀 쪽 존재로 톤만 이동.
3. **특수 조우 대사 3종** (커밋 `417cbce`)
   - `combat/battle-setup.js`의 `startBattle()` 맨 끝에 호출 1줄 추가 +
     신규 함수/데이터.
   - 회랑의 기사(paladin_knight) 전용: 회랑의 정령은 칼리버 X 단계별로
     다른 반응(인식→위화감→망각), 나머지 7종은 고정 대사. 런당 최대 2회.
   - 마녀의 시계(relic_witchclock) 보유 시: 직업 무관, 층별보스/회랑의
     몬스터 조우 시 내레이션 대사. 런당 최대 2회. 회랑의 기사 대사와 겹치면
     회랑의 기사 쪽 우선(같은 전투에서 오버레이 중복 방지).
   - 거울 보스: 잠식된 최종보스가 내 직업과 같으면 1회성 대사(빈도 조절 불필요).

## 최근 검증 상태

- `node --check`는 4개 커밋 전부 통과.
- Playwright 헤드리스 브라우저로 실제 검증 완료:
  - 오프닝 심리테스트 4개 동기 전체 흐름(문지기 신규 대사 포함) 정상 완료, JS 에러 0건
  - 물음표 이벤트 26개(25개 풀 + 조건부 재회) 전부 강제 호출해 렌더링/에러 확인 → 25개 정상, 1개(방랑 상인)는 깊이 0 특유의 기존 분기(무관)
  - 특수 조우 대사 9개 시나리오(정령 3단계, 재단사, 마녀의 시계 단독/중복, 거울 보스 성립/불성립) 직접 함수 호출로 로직 검증 — 전부 의도대로 동작
- **미검증**: 실제 수동 플레이(특히 칼리버 X 장비창 UI에서 desc 줄바꿈,
  진엔딩 grand 톤 페이드), 정예 특성이 붙은 "회랑의 ○○" 몬스터에서도
  특수 대사가 정상 발동하는지, 확률(50%)·상한(런당 2회)이 실제 플레이
  체감상 자연스러운지.
- 이전 세션(독사 밸런스)의 미검증/미해결 항목은 `HANDOFF.md`로 이동 —
  **독사의 구조적 한계(checkBattleEnd 타이밍 이슈)는 여전히 미해결.**

## 완료된 것으로 취급할 수 있는 영역

프로젝트에는 다음 핵심 시스템이 이미 존재한다.

- 기본 직업 시스템 / 하이브리드 2차 전직(전 직업 완성)
- 턴제 1:1 전투 / 몬스터·보스(층별보스, 일반/진 최종보스)
- 스킬 / 장비 / 유물 / 에픽 직업 각인 시스템(39개)
- 탐험 / 상점 / 대장간 / 저장·불러오기 / 기록·도감 / 상태창
- Web Audio 기반 사운드 / 몬스터 시각화
- **세계관 스토리 레이어**(오프닝 서사, 지역 묘사, 물음표 이벤트 25개,
  특수 조우 대사 3종) — 이번 세션에 완료

단, "완전히 테스트 완료"라는 의미는 아니다. 실제 테스트 여부는 해당 작업의 검증 결과를 따른다.

## 작업 시 주의

### 전투
전투 파일들은 서로 강하게 연결되어 있다. 작은 변경도 호출 순서와 턴 전환에 영향을 줄 수 있다. `player-actions.js`의 phys/magic 공용 대형 핸들러는 특례 분기가 얽혀있어 편집 후 반드시 재확인한다.

### 저장
저장 필드 변경은 `player.js`와 `storage.js`를 함께 확인한다. 단, `storage.js`가 `player` 객체를 통째로 직렬화하는 구조라(`JSON.stringify({player, ...})`), `player.xxx = player.xxx||기본값` 패턴으로 쓰는 새 필드는 두 파일을 안 건드려도 안전하다(이번 세션의 `corridorDialogueCount`/`witchClockDialogueCount`가 그 예).

### 직업
직업/전직 변경은 `jobs.js`, `job-advancement.js`, 필요 시 `skills.js`, `battle-end.js`를 함께 확인한다.

### 스토리/서사
`origin.js`(오프닝), `events.js`(물음표 이벤트), `combat/battle-setup.js`(특수 조우 대사), `combat/battle-end.js`(엔딩)에 서사 레이어가 분산돼 있다. 새 서사 요소 추가 시 이 네 파일이 기본 확인 대상.

### 몬스터
데이터/전투 생성/시각화가 분리되어 있으므로 작업 성격에 맞는 파일만 선택한다. 몬스터에는 desc/도감 필드가 아직 없다(도감 UI 자체가 없음) — 로어 텍스트를 넣으려면 UI 신설이 먼저 필요.

## 다음 작업

```text
### 1순위 — 브라우저 실전 테스트 (누적분 전체)
- 목표: 이번 세션의 스토리 기능 + 이전 세션의 미검증 항목(직업 리워크,
  각인 39개, 독사 구조적 한계)을 실제로 플레이해 확인
- 완료 조건: 각 직업 최소 1회씩 플레이, 회랑의 기사로 회랑의 정령을 3단계
  전부 만나보기, 마녀의 시계 보유 상태로 보스/회랑의 몬스터 조우

### 2순위 — 마녀 최종보스 구조 설계
- 목표: "마녀의 시계 보유 + 진 최종보스(회랑의 시조) 클리어 → 진짜 최종
  보스(마녀) 등장" 구조 확정. TRUE_FINAL_BOSS 위에 계층을 더 얹는 작업이라
  전투 흐름/엔딩 분기 구조를 건드리는 "매우 높음" 등급 — 설계안을 먼저
  제시하고 승인 후 구현할 것.
- 함께 정리할 것: 문지기=마녀 반전을 이 시점에 회수할지, 회수 방식(엔딩
  텍스트만 vs 별도 연출)

### 3순위 — 계약형 유물 4종 코드 구현
- 질주의 서약 / 정체된 맹세 / 결투자의 서약 / 무일푼의 각오(리턴값 미정)
- 스펙은 이미 확정돼 있음(과거 세션 기록 참고), 코드 반영만 남음

### 4순위 — 독사 구조적 한계 재검토
- `checkBattleEnd()` 타이밍 이슈로 인한 독틱 증발 문제. 전투 체인을
  건드려야 해서 리스크 큼 — 손댈지 여부부터 논의 필요
```

## 상태 업데이트 규칙

작업이 끝나면:

1. 완료된 작업을 추가한다.
2. 변경 파일을 기록한다.
3. 미검증 항목을 기록한다.
4. 다음 작업을 1~5개 이내로 적는다.
5. 오래된 완료 항목은 필요하면 별도 HANDOFF/CHANGELOG로 이동한다.

이 문서는 길게 만들지 않는다. 현재 상태만 유지한다.
