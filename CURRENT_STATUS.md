# CURRENT_STATUS.md — 현재 개발 상태

> 이 문서는 과거 대화의 대체물이다.
> 새 Claude 대화를 시작할 때 현재 상태를 빠르게 전달하기 위한 문서다.
> 오래된 상세 작업 이력은 `HANDOFF.md`를 참고한다.

## 기준 브랜치

- `main` 단독(과거 `mechanic-stoker-accumulator` 동시 반영 정책은 폐기됨)

## 현재 확인된 상태

- 프로젝트는 Vanilla JS/HTML/CSS 기반의 정적 브라우저 게임이다.
- 여러 classic `<script>`가 로드 순서에 따라 공유 전역 scope를 사용하는 구조다.
- 전투/탐험/직업/장비/유물/저장 등 기능이 여러 JS 파일로 분리되어 있다.
- 역병숙주(rogue_alchemist) 전용 에픽 각인 3종(`re_venomrush`/`re_venomburst`/`re_solovenom`)은
  이미 새 "기생형" 메커니즘(`enemy.venomStacks`, `getVenomDmgPerStack()`)에 맞춰
  재연결되어 있었다 — 예전 문서(jobs.js 코멘트)에 "미해결"이라고 적혀 있던 건
  낡은 정보였음(실제 코드가 최신).

## 최근 작업 (이번 세션 — 마녀 재도전 시스템)

> 직전 세션(2차 전직 밸런스/정예 체감 개선 등, 항목 18개)은 `HANDOFF.md`
> 맨 위로 이동했다.

### 1. 마녀(아이온) 조우 조건 강화 + 난이도별 배지 분리(왕관/마녀모자)
- 조건 변경: 마녀의 시계 보유 + 이번 판 무결 클리어만으로 아이온이 등장하던
  것을, **해당 난이도에서 회랑의 시조를 클리어한 기록이 최소 1회 있어야**
  하도록 강화. 미충족 시 지금처럼 회랑의 시조가 등장
- `combat/battle-setup.js`: `progenitorClearedThisDifficulty`(전역) 신설,
  `pickEnemy()`가 이 값과 마녀의 시계 보유 여부를 함께 확인
- `explore.js`: `renderFinalFloorStep()`이 전투 시작 직전 `loadRecords()`로
  판정해 위 값을 채워 넣음(동기 함수인 `pickEnemy()`를 위한 사전 로드)
- `ui/difficulty.js`/`bootstrap.js`: 마녀(Aiōn) 클리어 시 왕관 대신 마녀모자
  🎩 표시(`easy/normal/hardcoreWitchClear` 신설, `bossType`으로 왕관/마녀모자
  판정 분리)
- 변경 파일: `combat/battle-setup.js`, `explore.js`, `ui/difficulty.js`, `bootstrap.js`

### 2. 문지기 오프닝 — 마녀모자 난이도 전용 시계 제안 이벤트
- 해당 난이도에 마녀모자가 이미 있는 상태로 새 캐릭터를 생성하면, 문지기
  종료 대사가 더 짙은 버전(정체를 강하게 암시)으로 바뀌고 마녀의 시계를
  건넴 — "받는다/거절한다" 선택지, 각각 다른 반응 대사
- 수락 시 `player.relics`에 `relic_witchclock`을 즉시 추가(유물 슬롯 1칸을
  시작부터 소모)하고 `applyRelicEffect()` 호출
- 이름은 여전히 직접 밝히지 않음(간접 암시 원칙 유지) — 완전 공개는 3번(마녀
  2회차 엔딩)에서만
- 변경 파일: `origin.js`(`showGatekeeperClosing`/`showWitchClockOffer` 신설)

### 3. 마녀 2회차 클리어 전용 대화 엔딩
- 해당 난이도 마녀모자가 이미 있는 상태로 아이온을 다시 클리어하면
  (`isWitchRepeat`), 정체를 완전히 풀어놓는 전용 대화 시퀀스로 분기 —
  아코스를 잃은 두려움/역병/거짓 구원에 대한 회한 + 플레이어에게 건네는
  감사 인사. 1회차 엔딩(기존 짧은 진엔딩)은 그대로 유지
- 하드코어 전용 추가 대사 1줄: "…그리고 그대가 왔다" 바로 뒤에, 도망 불가
  + 사망 시 레벨1·1층 재시작이라는 이 난이도 특성을 그녀가 인정하는 대사
  삽입(`witchRepeatLines.splice`, 왕관 시너지 보너스 줄과 같은 패턴)
- 엔딩 화면 타이틀 "회랑, 두 번째로 놓아주다"로 별도 분기
- 변경 파일: `combat/battle-end.js`

### 4. 보통/하드코어 시작 유물 선택창
- 오프닝(문지기 대화) 종료 직후, 마을 진입 전에 기존 유물 제단 UI
  (`showRelicAltar`)를 재사용해 시작 유물을 직접 고르게 함
- `relics.js`: `showRelicAltar(atDepth, onDone)` — 선택/스킵/선택지 없음
  세 경로 전부 끝난 뒤 `onDone` 콜백을 1회 호출하도록 확장(기존 중간층
  호출부는 `onDone` 생략, 동작 그대로)
- `origin.js`: `applyOriginAndStart()`에서 쉬움이 아니면 `finishNewGameStart()`
  직전에 제단을 띄움 — `townCheckpoint` 생성보다 반드시 앞서 실행되도록
  순서 고정(안 그러면 사망 후 마을 복귀 시 시작 유물이 체크포인트에서 누락됨)
- 시작 골드(40G) < 스킵 비용(500G)이라 사실상 반드시 하나를 고르게 됨
- 변경 파일: `relics.js`, `origin.js`

### 커밋
`a00e5de`(1+2), `da3a64f`(3 기본), `09e4e2a`(4), `bce1dc4`(3 하드코어 추가 대사) — 전부 main에 푸시 완료.

### (보류/미착수) 유물 조합 시너지 + 서약 선택 시스템
같은 논의에서 나온 아래 두 안은 아직 구현 안 함:
- 유물 조합 시너지 4종 시안(A 회랑의 모든 유산 / B 네 개의 도박 / C 계약자 /
  D 축복받은 자) — 숫자 보상 없이 순수 토스트+히든 업적으로 가는 방향까지만
  합의, 실제 업적 데이터/토스트 코드는 아직 안 넣음
- 서약 선택 시스템(보통/하드코어에서 저주를 자원해서 짊어지고 확정 보상을
  받는 안) — 아이디어 단계, 수치/UI 미확정

## 최근 검증 상태

- 위 1~4(마녀 재도전 시스템) 전부 `node --check` 통과.
- **미검증**(브라우저 실전 확인 전부 안 됨):
  - 마녀 조우 조건 — 시조 클리어 기록 있음/없음 각각에서 실제로 시조/마녀가
    맞게 갈리는지
  - 난이도별 배지 전환(👑↔🎩) 실제 렌더
  - 문지기 시계 제안 — 수락/거절 양쪽 대사 흐름, 수락 시 유물 슬롯 반영
  - 마녀 2회차 엔딩 대사 흐름/페이드 타이밍(하드코어 삽입 줄 위치 포함)
  - 시작 유물 제단 — 마녀의 시계를 이미 받은 상태에서 남은 선택지가 정상
    표시되는지, `townCheckpoint`에 시작 유물이 포함돼 사망 후 마을 복귀 시
    유지되는지

## 완료된 것으로 취급할 수 있는 영역

프로젝트에는 다음 핵심 시스템이 이미 존재한다.

- 기본 직업 시스템 / 2차 전직 14종(밸런스 시뮬레이션 완료, 조정은 미착수)
- 턴제 1:1 전투 / 몬스터·보스(층별보스, 일반 최종보스, 진 최종보스 2종)
- 스킬 / 장비 / 유물(정체된 맹세/결투자의 서약 구현 완료, 질주의 서약/무일푼의 각오는 개발 안 함으로 확정) / 에픽 직업 각인 시스템(42개, 역병숙주 +3)
- 탐험 / 상점 / 대장간 / 저장·불러오기 / 기록·유물 도감·**몬스터 도감(신규)**·업적·상태창
- 실제 음원 기반 BGM(6모드) + SFX(9종, 하이브리드) / 몬스터 시각화
- 세계관 스토리 레이어(오프닝 서사, 지역 묘사, 물음표 이벤트 32개+조건부 3개,
  특수 조우 대사 다수, 아이온/시조 진최종보스 2종, 왕자/아코스 떡밥 회수 진행 중)
- **마녀(아이온) 재도전 시스템(신규, 브라우저 미검증)**: 조우 조건(시조
  클리어 기록 필요), 난이도별 배지(왕관/마녀모자 분리), 문지기 시계 제안
  이벤트(오프닝, 수락/거절), 마녀 2회차 전용 대화 엔딩(하드코어 추가 대사
  포함), 보통/하드코어 시작 유물 제단 — 문지기=아이온 떡밥은 이 시스템으로
  대부분 회수됨(9장 참고)
- UI 타이포그래피/버튼 리디자인(한글 정식 지원 폰트, 평면 버튼)

단, "완전히 테스트 완료"라는 의미는 아니다. 실제 테스트 여부는 해당 작업의 검증 결과를 따른다.

## 작업 시 주의

### 전투
전투 파일들은 서로 강하게 연결되어 있다. 작은 변경도 호출 순서와 턴 전환에 영향을 줄 수 있다. `player-actions.js`의 phys/magic 공용 대형 핸들러는 특례 분기가 얽혀있어 편집 후 반드시 재확인한다.

### 저장
저장 필드 변경은 `player.js`와 `storage.js`를 함께 확인한다. 단, `storage.js`가 `player` 객체를 통째로 직렬화하는 구조라(`JSON.stringify({player, ...})`), `player.xxx = player.xxx||기본값` 패턴으로 쓰는 새 필드는 두 파일을 안 건드려도 안전하다. 유물/몬스터 도감처럼 "계정 단위 영구 기록"은 `player` 세이브와 별개 키(`localStorage`/`window.storage`)로 관리한다(`RELICDEX_KEY`/`MONSTERDEX_KEY` 패턴 재사용).

### 사운드
`sound.js`가 BGM/SFX 둘 다 관장한다. BGM은 `<audio>` 엘리먼트, SFX는 `AudioBufferSourceNode` — 서로 다른 재생 메커니즘이니 혼동하지 않는다.

### UI/타이포그래피
제목류는 `var(--font-title)`(Gowun Batang), 그 외 전부는 `var(--font-ui)`(IBM Plex Sans KR). 색상 토큰은 `:root`의 기존 변수를 재사용한다.

### 직업
직업/전직 변경은 `jobs.js`, `job-advancement.js`, 필요 시 `skills.js`, `battle-end.js`를 함께 확인한다.

### 스토리/서사
`origin.js`(오프닝), `events.js`(물음표 이벤트), `combat/battle-setup.js`(특수 조우 대사·진최종보스 데이터), `combat/enemy-turn.js`(보스 스킬 실행), `combat/battle-end.js`(엔딩)에 서사 레이어가 분산돼 있다. 새 서사 요소 추가 시 이 파일들이 기본 확인 대상. `story.md`를 함께 갱신한다.

### 몬스터
데이터/전투 생성/시각화가 분리되어 있으므로 작업 성격에 맞는 파일만 선택한다. `data/monsters.js`의 `dex` 필드(신규)에 도감용 플레이버 텍스트가 있다 — 새 몬스터 추가 시 이 필드도 함께 채운다.

## 다음 작업

```text
### 1순위 — 마녀 재도전 시스템 브라우저 실전 검증
- 위 "최근 검증 상태" 미검증 항목 전부(가장 최근 작업이라 우선)

### 2순위 — 보통/하드코어 재미 요소 후속 (아이디어 단계, 미착수)
- 유물 조합 시너지 4종(A 회랑의 모든 유산/B 네 개의 도박/C 계약자/D 축복받은
  자) — 숫자 보상 없이 히든 업적+토스트로 가는 방향까지 합의, 구현 전
- 서약 선택 시스템(보통/하드코어에서 저주를 자원해서 짊어지고 확정 보상을
  받는 안) — 수치/UI 미확정

### 3순위 — 2차 전직 밸런스 조정
- 황금 도박사: 에픽셋 연결/연패 완화/각인 완화는 완료 — 브라우저 실전 검증
  필요. 판돈 스노우볼 자체(성공 시 판돈 2배 재투자)는 아직 미조정
- 사기꾼(데미지 액티브 부재)은 아직 미착수

### 4순위 — 역병숙주 구조적 한계 재검토
- checkBattleEnd() 타이밍 이슈로 인한 독틱 증발 문제(전투 체인을 건드려야 해서 리스크 큼)

### 5순위 — 기타 후순위
- 몬스터 도감에 진최종보스/층별보스 포함 여부 검토
- 남은 SFX 슬롯(guard/poisonHit/fail/click/victory/gameOver/statusApply) 음원 확보 여부 확인
```

> 질주의 서약 / 무일푼의 각오(계약형 유물 2종)는 사용자 요청으로 할일 목록에서
> 완전히 제외됨 — 더 이상 개발하지 않는다.

## 상태 업데이트 규칙

작업이 끝나면:

1. 완료된 작업을 추가한다.
2. 변경 파일을 기록한다.
3. 미검증 항목을 기록한다.
4. 다음 작업을 1~5개 이내로 적는다.
5. 오래된 완료 항목은 필요하면 별도 HANDOFF/CHANGELOG로 이동한다.

이 문서는 길게 만들지 않는다. 현재 상태만 유지한다.
