# CURRENT_STATUS.md — 현재 개발 상태

> 이 문서는 과거 대화의 대체물이다.
> 새 Claude 대화를 시작할 때 현재 상태를 빠르게 전달하기 위한 문서다.
> 오래된 상세 작업 이력은 `HANDOFF.md`를 참고한다.

## 기준 브랜치

- `main` 단독(과거 `mechanic-stoker-accumulator` 동시 반영 정책은 폐기됨)

## 현재 확인된 상태

- 프로젝트는 Vanilla JS/HTML/CSS 기반의 정적 브라우저 게임이다.
- 여러 classic `<script>`가 로드 순서에 따라 공유 전역 scope를 사용하는 구조다.
- 전투/탐험/직업/장비/유물/저장 등 기능이 여러 JS 파일로 분리되어 있다.
- 역병숙주(rogue_alchemist) 전용 에픽 각인 3종(`re_venomrush`/`re_venomburst`/`re_solovenom`)은
  이미 새 "기생형" 메커니즘(`enemy.venomStacks`, `getVenomDmgPerStack()`)에 맞춰
  재연결되어 있었다 — 예전 문서(jobs.js 코멘트)에 "미해결"이라고 적혀 있던 건
  낡은 정보였음(실제 코드가 최신).

## 최근 작업 (이번 세션)

### 1. 2차 전직 14종 Monte Carlo 밸런스 시뮬레이션
- 레벨15/10턴 고정 조건, 방어 6/14/22 3구간 × 20,000회
- 결과: 황금 도박사(판돈 스노우볼로 과도하게 강함), 사기꾼(데미지 액티브 자체가
  없는 구조적 문제) 2곳이 이상치로 발견됨 — **밸런스 조정은 아직 미착수**

### 2. 역병숙주 각인 확장
- 기존 3종에 `exclusiveGroup`(슬롯당 택1) 부여 + 신규 3종 추가:
  폭식의 각인(무기)/잠식 갑주(방어구)/공생의 각인(장신구)
- 변경 파일: `blacksmith.js`, `player-actions.js`, `enemy-turn.js`, `equipment.js`, `jobs.js`

### 3. 찰나의 검사 삼박난무 VFX 페이싱 조정
- 3연타(80ms 균일) → 10연타(느림2+빠름8: `[0,300,370,440,510,580,650,720,790,860]`ms)
- `spawnFigureSlashFx()`의 외곽 좌표 제한 제거(참격과 동일한 중앙 근처 범위 사용)
- 변경 파일: `player-actions.js`, `battle-fx.js`

### 4. 왕자 떡밥 3단계 세트 + 문지기=아이온/아코스 균형 보강
- 잭(jack) 전용 3단계 escalating 대사(`maybeShowJackPrinceDialogue`, 직업 무관)
- 물음표 이벤트 "잠긴 육아실"(잭 대사 3단계 완주 후 등장) → 신규 장신구
  `r_woodenhorse` 지급, 착용 중 잭 재조우 시 1회성 인식 대사
- 진엔딩(시조 버전)에 손자국 한 줄 추가
- 신규 이벤트 "닫히지 않는 문"(문지기=아이온 반전 회수 1단계), "부서진 갑주
  걸이"(아코스 균형, 회랑의 기사 전용 문구), "부서진 톱니 장신구"(아이온 파편,
  마녀의 시계 보유자 전용, 신규 장신구 `r_gearshard`)
- "회랑의 ○○" 미조명 4종(재단사/금서/굴착꾼/어릿광대) 소규모 조명 이벤트 추가
- 물음표 이벤트 정리: 그림자와의 결투/피의 제단/수상한 무기 3종 제거(테마 중복)
  → 최종 32개(균등확률) + 조건부 3개
- 역병숙주-회랑의 시조 조우 토스트 추가
- 변경 파일: `events.js`, `combat/battle-setup.js`, `combat/battle-end.js`, `data/equipment.js`, `story.md`

### 5. 몬스터 도감 신설
- `data/monsters.js`의 MONSTERS 20종에 `dex`(짧은 플레이버 텍스트) 필드 추가
- `storage.js`: `MONSTERDEX_KEY`/`loadMonsterDex()`/`addToMonsterDex()` 신설(유물 도감과 동일 패턴, 계정 단위 영구 기록)
- `combat/battle-setup.js`의 `startBattle()`에서 조우 시점에 자동 등록(처치 여부 무관)
- `records.js`: `showMonsterDex()` UI 신설(유물 도감과 동일한 잠금/해제 카드 레이아웃)
- `bootstrap.js`/`index.html`: "🐾 몬스터 도감" 버튼 추가(유물 도감 옆)
- 진최종보스/층별보스는 이번 1차 범위에서 제외(일반 몬스터 20종만)

### 6. admin3 유물 개수 버그 수정
- 무작위 유물 추첨(n개) 후 마녀의 시계를 무조건 추가 지급하던 방식이라,
  추첨에서 마녀의 시계가 안 뽑히면 총 n+1개가 되던 버그(사용자 제보 — 하드코어
  기준 5개). 마녀의 시계 몫으로 슬롯 하나를 미리 비워두고(n-1개만 무작위
  추첨) 마지막에 채우도록 수정 — 항상 정확히 n개(난이도별 슬롯 수와 동일)
- 변경 파일: `explore.js`

### 7. 거꾸로 된 왕관 시너지 세트
- 획득 시 전용 토스트, 회랑의 시조(무결 클리어) 조우 시 전용 대사, 진엔딩
  보너스 한 줄(왕관 옆에 거꾸로 뒤집힌 왕관이 나란히 놓임)
- 변경 파일: `relics.js`, `combat/battle-setup.js`, `combat/battle-end.js`, `story.md`

### 8. 황금 도박사 밸런스 수정(사용자 실플레이 제보 기반)
- **에픽셋 연결 버그 수정**: `jesterGoldBet`/`jesterAllIn`(베팅/올인)에 `luck:true`가
  없어서 도박사 3세트("운명의 마지막 패")의 잭팟 게이지/90% 성공률 보장이
  전혀 적용되지 않던 버그. `epicLuckPre/epicLuckApplyChance/epicLuckPost`를
  다른 luck 스킬들과 동일하게 연결
- **레벨12 저확률 연패 완화(A안)**: 마스터리 "물주의 감각"에 연패 보정 추가
  — 베팅/올인이 연속 실패할 때마다 다음 판 성공률 +10%p씩 누적(최대 3연패
  +30%p), 성공 시 초기화
- **사기 정보 각인 완화**: 비용 배율 2배(40%→80%) → 1.5배(40%→60%)로 하향
  (보너스는 2배 그대로 유지). 기존엔 사실상 못 쓰는 각인이었음
- 변경 파일: `data/skills.js`, `combat/player-actions.js`, `blacksmith.js`

### 9. 데미지 숫자 겹침 버그 수정(기관사 로봇 배치 시)
- 기존엔 -35~35px 완전 무작위 오프셋이라, 로봇 슬롯 3개(rig/rig2/omegaRig)가
  짧은 간격(~700ms)으로 연속 사격하면 우연히 비슷한 위치가 나와 겹쳐 보이는
  경우가 있었음(사용자 제보). 화면에 떠 있는 숫자들의 위치를 추적해 겹치지
  않는 오프셋을 순환식으로 고르도록 수정(`popDamage()`)
- 변경 파일: `combat/battle-fx.js`

### 10. 폭주 화부 압력 게이지 개선 + 폭주 사출 VFX(방향 2)
- 텍스트뿐이던 압력 게이지를 실제 바(fill) 형태로 변경, 폭주 압력(마스터리)
  보유 시 실제 상한(150)을 그대로 분모로 쓰고 100 지점엔 항상 붉은 경계선을
  그어 "여기부터 초과분 구간"임을 시각적으로 구분(기존엔 항상 "/100"으로
  표시돼 실제 상한 150과 헷갈렸음 — 사용자 제보)
- 폭주 사출(mechanicOverloadDischarge) 발동 시 게이지가 순간 밝아졌다
  가라앉는 스파이크 연출 추가(새 이미지 리소스 없이 기존 게이지만 활용)
- 변경 파일: `index.html`, `combat/battle-fx.js`, `combat/player-actions.js`

### 11. 상태창 스킬 목록 패시브/액티브 구분
- 1각/2각 구분(테두리 색)과 겹치지 않도록, 아이콘(⚙ 패시브 / ⚔ 액티브)
  + 무채색 태그(실선/점선 테두리)로 종류를 구분
- 변경 파일: `combat/battle-fx.js`, `index.html`

### 12. 역병숙주 전용 화면 스모그
- 잠식 스택(enemy.venomStacks) 비율에 비례해 전투 화면 전체에 초록빛
  비네트가 은은히 짙어지는 연출 추가(최대 불투명도 0.55로 캡, 가독성 유지)
- `renderStatus()`(explore.js)가 상태 갱신마다 자동으로 갱신
- 변경 파일: `index.html`, `combat/battle-fx.js`, `explore.js`

### 13. 폭주 화부 궁극기 타이밍 개선(사용자 제보 — "임계 폭주를 쓸 타이밍이 안 나온다")
- 원인: 압력이 폭주 사출 사용(+25)이나 포탑 틱으로만 올라서, 100(임계 폭주
  최소 요구치)에 도달할 즈음엔 이미 전투가 끝나 있는 경우가 잦았음
- 해결(사용자 제안): 마스터리 "폭주 압력"에 매 라운드 자동 압력 상승분
  (`passiveGainPerTurn:10`)을 추가 — 다른 행동을 하는 턴에도 압력이 꾸준히
  쌓여 궁극기 발동 타이밍이 실제 전투 흐름 안으로 앞당겨짐. 기존 소스(포탑
  틱/폭주사출)와 완전히 별개로 가산되어 스노우볼 자체는 그대로 유지
- 변경 파일: `data/skills.js`, `combat/enemy-turn.js`

### 14. 폭주 화부 자동방출과 임계 폭주 충돌 버그 수정(사용자 제보 — "압력 110에서 다음 턴에 갑자기 0이 됨")
- 원인: `checkMechanicOverheat()`(압력 100 이상으로 내 턴이 돌아오면 자동
  강제 방출 + 리셋)가 원래 1차 기관사 기본 트리용 안전장치인데 전직 구분
  없이 모든 기관사에게 걸려 있었음. 폭주 화부는 정반대로 100을 일부러
  넘겨 들고 있다가 임계 폭주로 직접 터뜨리는 게 핵심 정체성이라 정면으로
  충돌 — 직전 세션에 추가한 "매턴 자동 압력 상승"이 오히려 이 충돌을 더
  자주 만나게 만들었음
- 해결: `mastery_overheat`(폭주 화부 마스터리) 보유자는 이 레거시
  자동방출에서 제외
- 변경 파일: `combat/battle-fx.js`

### 15. 역병숙주 스모그 범위 버그 수정(사용자 제보 — "전투화면 전체가 아니라 화면 전체를 덮어버림")
- 원인: `.screen-smog`가 `position:fixed`라 메시지창/명령 버튼까지 포함해
  전투 화면 전체(사실상 뷰포트 전체)를 덮고 있었음
- 해결: `.archway`(몬스터가 있는 장면 영역, position:relative+overflow:hidden)
  내부로 옮기고 `position:absolute`로 변경 — 이제 장면 영역에만 한정됨
- 변경 파일: `index.html`

### 16. 전투 중 스킬 목록에도 패시브/액티브 구분 추가(사용자 요청 — 상태창뿐 아니라 이쪽도 필요했음)
- 상태창(`openStatusSheet`)에만 먼저 넣었던 ⚙ 패시브 / ⚔ 액티브 태그를
  전투 중 스킬 선택 목록(`openSub('skill')`)에도 동일하게 추가
- 변경 파일: `combat/battle-fx.js`

### 17. 정예 몬스터 대비 체감 개선 — 수치 너프 + 예고 시스템(사용자 기획, 여러 세션에 걸친 논의)
- 배경: 서프라이즈 정예(2~3특성) 조우 시 포션 먹기도 전에 훅 가는 문제.
  "불사조의 깃털"(아이템) 같은 강제 대처 아이템 없이, 랜덤성은 유지하되
  대처 여지를 주는 방향으로 설계
- **수치 너프**: 철갑 첫2턴→첫1턴, 사냥꾼 +40%→+30%, 반사 15%(무제한)→
  15%(1회당 플레이어 최대HP 10% 상한), 마나포식 -2→-5(이건 상향)
- **예고 시스템 3갈래**:
  - A(주기형, 완전예측 가능): 광기(3턴 주기) — 발동 1턴 전에 토스트 경고
    (`combat/battle-fx.js`의 `resetCommandUI()`, 보스 예고 인프라와 유사한
    "정확히 1번만" 보장 지점 재사용)
  - B(문턱형, 즉시알림): 사냥꾼(HP30%↓)/광폭(적HP50%↓) — 조건 진입 순간
    전투당 1회 토스트(`explore.js`의 `renderStatus()`)
  - C(반응형, 상태뱃지): 복수 — `revengeArmed` 상태를 상태뱃지로 상시 표시
    (`combat/battle-fx.js`의 `updateStatusBadges()`)
  - D(지속형/패시브): 흡혈/재생/불사/저주/독성/마나포식 등은 예고 불필요
    (전투 시작 전 안내로 충분)
- **보류(미착수)**: 정예 버스트 특성 조합 곱연산 상한(4번안) — "킵" 상태로
  아직 개발 안 함
- 변경 파일: `combat/enemy-turn.js`, `combat/battle-setup.js`, `combat/battle-fx.js`, `combat/player-actions.js`, `explore.js`

### 18. 정예 버스트 특성 곱연산 상한 — 방식2(체감 감쇠) 구현
- 이전 세션에 "킵"해둔 4번안을 방식2(체감 감쇠)로 확정 개발
- 광폭/사냥꾼/복수/광기 중 여러 개가 같은 턴에 동시 발동해도, 예전처럼
  전부 곱연산으로 쌓이지 않고 배율이 큰 순서대로 100%/70%/50%만 반영하는
  합연산으로 변경 — 단일 특성 위력은 그대로, 최악의 조합만 완화
  (3특성 최악 기준 ×2.7 → ×1.96, 2특성 최악 기준 ×2.08 → ×1.81)
- 변경 파일: `combat/enemy-turn.js`(`getEffectiveEnemyAtk()`)

## 최근 검증 상태

- 위 1~5 전부 `node --check` 통과.
- **미검증**(브라우저 실전 확인 전부 안 됨):
  - 대장간에서 역병숙주 신규 각인 3종이 실제로 후보로 뜨는지, exclusiveGroup 필터링
  - 삼박난무 VFX 실제 재생 타이밍/느낌
  - 잭 대사 3단계 → 잠긴 육아실 등장 → 재조우 흐름 전체
  - 마녀의 시계 게이트(부서진 톱니 장신구), 회랑의 기사 보너스 문구
  - 몬스터 도감 등록/UI 렌더링, `window.storage` vs `localStorage` 폴백 양쪽

## 완료된 것으로 취급할 수 있는 영역

프로젝트에는 다음 핵심 시스템이 이미 존재한다.

- 기본 직업 시스템 / 2차 전직 14종(밸런스 시뮬레이션 완료, 조정은 미착수)
- 턴제 1:1 전투 / 몬스터·보스(층별보스, 일반 최종보스, 진 최종보스 2종)
- 스킬 / 장비 / 유물(정체된 맹세/결투자의 서약 구현 완료, 질주의 서약/무일푼의 각오는 개발 안 함으로 확정) / 에픽 직업 각인 시스템(42개, 역병숙주 +3)
- 탐험 / 상점 / 대장간 / 저장·불러오기 / 기록·유물 도감·**몬스터 도감(신규)**·업적·상태창
- 실제 음원 기반 BGM(6모드) + SFX(9종, 하이브리드) / 몬스터 시각화
- 세계관 스토리 레이어(오프닝 서사, 지역 묘사, 물음표 이벤트 32개+조건부 3개,
  특수 조우 대사 다수, 아이온/시조 진최종보스 2종, 왕자/아코스/문지기 떡밥 회수 진행 중)
- UI 타이포그래피/버튼 리디자인(한글 정식 지원 폰트, 평면 버튼)

단, "완전히 테스트 완료"라는 의미는 아니다. 실제 테스트 여부는 해당 작업의 검증 결과를 따른다.

## 작업 시 주의

### 전투
전투 파일들은 서로 강하게 연결되어 있다. 작은 변경도 호출 순서와 턴 전환에 영향을 줄 수 있다. `player-actions.js`의 phys/magic 공용 대형 핸들러는 특례 분기가 얽혀있어 편집 후 반드시 재확인한다.

### 저장
저장 필드 변경은 `player.js`와 `storage.js`를 함께 확인한다. 단, `storage.js`가 `player` 객체를 통째로 직렬화하는 구조라(`JSON.stringify({player, ...})`), `player.xxx = player.xxx||기본값` 패턴으로 쓰는 새 필드는 두 파일을 안 건드려도 안전하다. 유물/몬스터 도감처럼 "계정 단위 영구 기록"은 `player` 세이브와 별개 키(`localStorage`/`window.storage`)로 관리한다(`RELICDEX_KEY`/`MONSTERDEX_KEY` 패턴 재사용).

### 사운드
`sound.js`가 BGM/SFX 둘 다 관장한다. BGM은 `<audio>` 엘리먼트, SFX는 `AudioBufferSourceNode` — 서로 다른 재생 메커니즘이니 혼동하지 않는다.

### UI/타이포그래피
제목류는 `var(--font-title)`(Gowun Batang), 그 외 전부는 `var(--font-ui)`(IBM Plex Sans KR). 색상 토큰은 `:root`의 기존 변수를 재사용한다.

### 직업
직업/전직 변경은 `jobs.js`, `job-advancement.js`, 필요 시 `skills.js`, `battle-end.js`를 함께 확인한다.

### 스토리/서사
`origin.js`(오프닝), `events.js`(물음표 이벤트), `combat/battle-setup.js`(특수 조우 대사·진최종보스 데이터), `combat/enemy-turn.js`(보스 스킬 실행), `combat/battle-end.js`(엔딩)에 서사 레이어가 분산돼 있다. 새 서사 요소 추가 시 이 파일들이 기본 확인 대상. `story.md`를 함께 갱신한다.

### 몬스터
데이터/전투 생성/시각화가 분리되어 있으므로 작업 성격에 맞는 파일만 선택한다. `data/monsters.js`의 `dex` 필드(신규)에 도감용 플레이버 텍스트가 있다 — 새 몬스터 추가 시 이 필드도 함께 채운다.

## 다음 작업

```text
### 1순위 — 2차 전직 밸런스 조정
- 황금 도박사: 에픽셋 연결/연패 완화/각인 완화는 이번 세션에 수정 완료 —
  브라우저 실전 검증 필요. 판돈 스노우볼 자체(성공 시 판돈 2배 재투자)는
  아직 미조정
- 사기꾼(데미지 액티브 부재)은 아직 미착수

### 2순위 — 이번 세션 신규 콘텐츠 브라우저 실전 검증
- 위 "최근 검증 상태" 미검증 항목 전부

### 3순위 — 역병숙주 구조적 한계 재검토
- checkBattleEnd() 타이밍 이슈로 인한 독틱 증발 문제(전투 체인을 건드려야 해서 리스크 큼)

### 4순위 — 기타 후순위
- 몬스터 도감에 진최종보스/층별보스 포함 여부 검토
- 남은 SFX 슬롯(guard/poisonHit/fail/click/victory/gameOver/statusApply) 음원 확보 여부 확인
```

> 질주의 서약 / 무일푼의 각오(계약형 유물 2종)는 사용자 요청으로 할일 목록에서
> 완전히 제외됨 — 더 이상 개발하지 않는다.

## 상태 업데이트 규칙

작업이 끝나면:

1. 완료된 작업을 추가한다.
2. 변경 파일을 기록한다.
3. 미검증 항목을 기록한다.
4. 다음 작업을 1~5개 이내로 적는다.
5. 오래된 완료 항목은 필요하면 별도 HANDOFF/CHANGELOG로 이동한다.

이 문서는 길게 만들지 않는다. 현재 상태만 유지한다.
