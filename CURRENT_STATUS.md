# CURRENT_STATUS.md — 현재 개발 상태

> 이 문서는 과거 대화의 대체물이다.
> 새 Claude 대화를 시작할 때 현재 상태를 빠르게 전달하기 위한 문서다.
> 오래된 상세 작업 이력은 `HANDOFF.md`를 참고한다.

## 기준 브랜치

- `main` (이번 세션도 `main`/`mechanic-stoker-accumulator` 양쪽에 동일 반영)

## 현재 확인된 상태

- 프로젝트는 Vanilla JS/HTML/CSS 기반의 정적 브라우저 게임이다.
- 여러 classic `<script>`가 로드 순서에 따라 공유 전역 scope를 사용하는 구조다.
- 전투/탐험/직업/장비/유물/저장 등 기능이 여러 JS 파일로 분리되어 있다.
- `js/`에는 기존 문서에 적힌 파일 외에도 `events.js`, `nodemap.js`, `origin.js`가 존재한다.
- 기존 `PROJECT.md`는 일부 최신 파일/구조보다 뒤처져 있으므로 실제 코드를 우선한다.

## 현재 상태 요약

이번 세션은 **"시간의 마녀"(아이온) 진 최종보스 신설**이 핵심이었다.
설정: 왕(회랑의 시조)에게 마녀의 시계를 건넨 마녀 본인 — 마녀의 시계를
든 채 진 최종보스 조우에 도달하면, 대역(시조)을 보낼 필요 없이 그녀가
직접 나선다는 서사. `TRUE_FINAL_BOSS` 위에 조건부 계층을 얹는 "매우 높음"
등급 작업이었지만, 기존 광폭화/최후의 발악/포즈 전환 시스템을 최대한
재사용해 전투 로직 체인 자체는 건드리지 않았다. 이전 세션(오프닝/물음표
이벤트/특수 조우 대사) 상세는 `HANDOFF.md`로 이동했다.

## 최근 작업 (3개 커밋)

1. **시간술사(mage_time) 스킬에 아이온 간접 힌트** (`10a8125`) — 마스터리
   "시간 왜곡"과 마녀의 시계 유물이 완전히 동일한 메커니즘이라는 기존
   개발자 주석에 착안, 마스터리/레벨15 궁극기 desc에 "Aiōn" 이름이 희미하게
   스치는 문장 추가(칼리버X와 같은 방식 — 직접 언급 없음).
2. **멈춘 시계공방 이벤트 + 시간술사/아이온 공명 조우 대사** (`68ca03c`) —
   물음표 이벤트 26번째 신설(마녀의 시계 보유 시 4번째 선택지). 회랑의
   기사 전용이던 조우 대사 함수를 시간술사도 함께 쓰도록 통합, 시간술사+
   마녀의 시계 동시 보유 시 별도 "아이온 공명" 대사(런당 1회) 추가.
3. **진 최종보스 "시간의 마녀"(아이온) 전체 구현** (`a12fc6d`) — 아래 상세.

### 아이온 구현 상세 (`a12fc6d`)

- **스탯/스킬**: `TRUE_FINAL_BOSS_WITCH`(HP440/ATK34/DEF20/SPD14, 시조보다
  덜 단단하고 빠름). 스킬은 시간술사 3종에 대응— 가속의 손(텔레그래프
  강공격), 시간의 역설(매 턴 누적되는 "시간 파편" 스택 최대5, 소비 시
  1.0+0.6×스택 배율 — 플레이어 시간술사 궁극기와 동일 공식).
- **광폭화**: `ENRAGE_STEPS_WITCH` 신설, "시간 역행 — 되감긴 시간이 상처를
  지운다" 라벨. `canEnrage()`/`triggerEnragePhase()`/`checkLastStand()` 3곳
  전부 새 헬퍼 `getEnrageSteps(e)`로 통합(발동 조건/부활 로직 자체는
  `ENRAGE_STEPS_TRUE`와 동일한 기존 코드 재사용, 라벨만 분기).
  실제로 이미 있던 "부활 시 풀피 회복" 로직이 "시간을 역행해 체력이 다시
  찬다"는 컨셉과 정확히 일치해 추가 구현이 필요 없었다.
- **이미지**: 사용자 제공 3장(idle/telegraph/slam)을 PIL로 흰 배경 제거
  (`white_dist` 임계값 70)해 `images/monsters/timewitch*.png`로 저장.
  `monster-visuals.js`에 `TIMEWITCH_POSE_IMG`/`BOSS_POSE_IMG_BY_TYPE` 추가,
  `battle-fx.js`의 `setBossPoseImage()`를 progenitor 전용에서 여러
  진최종보스를 지원하도록 일반화.
- **후광**: `index.html`에 보라색 `.witch-boss-halo` CSS 신설(기존
  `.true-boss-halo`와 애니메이션 공유, 색상만 다름).
- **엔딩 분기**: `battle-end.js`의 `showEnding(true)`를 `enemy.type`으로
  분기 — 아이온 버전은 왕관 대신 "시계가 산산조각", 백성 원혼 대사도
  "이제야 놓아줄 수 있게 되었다"로 변경. 타이틀도 "회랑, 마침내 시간을
  되찾다"로 별도.
- **admin2/admin3 시작 위치 조정**: 기존엔 "고요한 제단" 준비 노드를 이미
  통과한 것으로 두고 보스 노드만 남겼는데, 이제 그 준비 노드부터 정상
  진행하도록 "한 마을 전"(`player.nodeMap=null` 등 미리 만들지 않음)에서
  시작 — `onAdvance()`의 기존 "지도 없으면 새로 생성" 로직을 그대로 탐.

## 최근 검증 상태

- `node --check` 전체 파일 통과.
- Playwright 헤드리스로 실제 실행 검증:
  - `pickEnemy(false,true,true)`가 마녀의 시계 보유/미보유에 따라 정확히
    아이온/시조로 분기
  - admin2로 로그인 → "나아가기" → 고요한 제단 지도(준비노드+보스노드)
    정상 생성 확인
  - 실전 전투 진입 시 이미지(`timewitch.png`)·보라 후광(`witch-boss-halo`)
    정상 적용, telegraph/slam 포즈 전환 정상
  - 시간의 역설 스택 배율 공식 검증(스택0=79 vs 스택4=269)
  - 광폭화 라벨 분기(아이온="시간 역행", 시조="각성") 검증
  - **전체 완주 플레이스루**(normal 난이도, admin2 → 준비노드 통과 →
    최종전투 진입 → 광폭화 실제 발동(HP 30→33, phase 0→1 확인) → 승리 →
    엔딩 화면 도달, 타이틀 "회랑, 마침내 시간을 되찾다" 확인)까지 완료
  - 기존 회귀(오프닝 4종, 물음표 이벤트 26개, 회랑의 기사/시간술사 조우
    대사) 전부 재확인, 정상
- **미검증**: 하드코어 난이도에서의 아이온 밸런스, 최후의 발악(30% HP
  구간) 실제 발동 장면(테스트에선 오버킬로 한 방에 지나쳐 관찰 못함 —
  시조도 동일 조건이면 같은 현상이라 별도 이슈 아님), 정예 특성이 붙은
  "회랑의 ○○" 몬스터에서 특수 조우 대사가 정상 발동하는지(이전 세션부터
  이어진 미검증 항목).
- 이전 세션들(스토리 통합 1부, 독사 밸런스)의 상세는 `HANDOFF.md`로 이동
  — **독사의 구조적 한계(checkBattleEnd 타이밍 이슈)는 여전히 미해결.**

## 완료된 것으로 취급할 수 있는 영역

프로젝트에는 다음 핵심 시스템이 이미 존재한다.

- 기본 직업 시스템 / 하이브리드 2차 전직(전 직업 완성)
- 턴제 1:1 전투 / 몬스터·보스(층별보스, 일반 최종보스, **진 최종보스 2종**
  — 회랑의 시조/시간의 마녀, 마녀의 시계 보유 여부로 분기)
- 스킬 / 장비 / 유물 / 에픽 직업 각인 시스템(39개)
- 탐험 / 상점 / 대장간 / 저장·불러오기 / 기록·도감 / 상태창
- Web Audio 기반 사운드 / 몬스터 시각화
- **세계관 스토리 레이어**(오프닝 서사, 지역 묘사, 물음표 이벤트 26개,
  특수 조우 대사 5종 — 회랑의 기사/시간술사/마녀의 시계/거울 보스/아이온
  공명, 아이온 보스 전체)

단, "완전히 테스트 완료"라는 의미는 아니다. 실제 테스트 여부는 해당 작업의 검증 결과를 따른다.

## 작업 시 주의

### 전투
전투 파일들은 서로 강하게 연결되어 있다. 작은 변경도 호출 순서와 턴 전환에 영향을 줄 수 있다. `player-actions.js`의 phys/magic 공용 대형 핸들러는 특례 분기가 얽혀있어 편집 후 반드시 재확인한다.

### 저장
저장 필드 변경은 `player.js`와 `storage.js`를 함께 확인한다. 단, `storage.js`가 `player` 객체를 통째로 직렬화하는 구조라(`JSON.stringify({player, ...})`), `player.xxx = player.xxx||기본값` 패턴으로 쓰는 새 필드는 두 파일을 안 건드려도 안전하다.

### 직업
직업/전직 변경은 `jobs.js`, `job-advancement.js`, 필요 시 `skills.js`, `battle-end.js`를 함께 확인한다.

### 스토리/서사
`origin.js`(오프닝), `events.js`(물음표 이벤트), `combat/battle-setup.js`(특수 조우 대사·진최종보스 데이터), `combat/enemy-turn.js`(보스 스킬 실행), `combat/battle-end.js`(엔딩)에 서사 레이어가 분산돼 있다. 새 서사 요소 추가 시 이 파일들이 기본 확인 대상.

### 진 최종보스 확장
새 진최종보스를 추가하려면: ① `battle-setup.js`에 스탯 데이터 + `pickEnemy()`의 `isTrueFinal` 분기에 조건 추가, ② 광폭화가 필요하면 `ENRAGE_STEPS_*` 배열 신설 후 `getEnrageSteps()`에 분기 추가(발동 로직 자체는 공용), ③ 포즈 이미지가 있으면 `monster-visuals.js`의 `BOSS_POSE_IMG_BY_TYPE`에 등록, ④ 후광 색상이 다르면 `index.html`에 새 CSS 클래스, ⑤ 엔딩 텍스트가 다르면 `battle-end.js`의 `showEnding()`에서 `enemy.type` 분기 추가. 전투 턴 흐름(`enemyTurn`/`checkBattleEnd`) 자체는 건드릴 필요 없다.

### 몬스터
데이터/전투 생성/시각화가 분리되어 있으므로 작업 성격에 맞는 파일만 선택한다. 몬스터에는 desc/도감 필드가 아직 없다(도감 UI 자체가 없음) — 로어 텍스트를 넣으려면 UI 신설이 먼저 필요.

## 다음 작업

```text
### 1순위 — 계약형 유물 4종 코드 구현
- 질주의 서약 / 정체된 맹세 / 결투자의 서약 / 무일푼의 각오(리턴값 미정)
- 스펙은 이미 확정돼 있음(과거 세션 기록 참고), 코드 반영만 남음

### 2순위 — 독사 구조적 한계 재검토
- checkBattleEnd() 타이밍 이슈로 인한 독틱 증발 문제. 전투 체인을
  건드려야 해서 리스크 큼 — 손댈지 여부부터 논의 필요

### 3순위 — 아이온 밸런스 실전 검증
- 하드코어 난이도 포함, 자연 진행(비-admin) 캐릭터로 실제 클리어 시도
- 최후의 발악 구간이 실제로 유의미하게 보이는지(오버킬 아닌 일반 화력 기준)

### 4순위 — 정예 "회랑의 ○○" 몬스터 특수 대사 발동 확인
- 이전 세션부터 이어진 미검증 항목 — 정예 특성이 붙어도
  maybeShowCorridorEncounterDialogue() 등이 정상 동작하는지
```

## 상태 업데이트 규칙

작업이 끝나면:

1. 완료된 작업을 추가한다.
2. 변경 파일을 기록한다.
3. 미검증 항목을 기록한다.
4. 다음 작업을 1~5개 이내로 적는다.
5. 오래된 완료 항목은 필요하면 별도 HANDOFF/CHANGELOG로 이동한다.

이 문서는 길게 만들지 않는다. 현재 상태만 유지한다.
