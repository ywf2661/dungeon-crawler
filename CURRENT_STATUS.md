# CURRENT_STATUS.md — 현재 개발 상태

> 이 문서는 과거 대화의 대체물이다.
> 새 Claude 대화를 시작할 때 현재 상태를 빠르게 전달하기 위한 문서다.
> 오래된 상세 작업 이력은 `HANDOFF.md`를 참고한다.

## 기준 브랜치

- `main` 단독(과거 `mechanic-stoker-accumulator` 동시 반영 정책은 폐기됨)

## 현재 확인된 상태

- 프로젝트는 Vanilla JS/HTML/CSS 기반의 정적 브라우저 게임이다.
- 여러 classic `<script>`가 로드 순서에 따라 공유 전역 scope를 사용하는 구조다.
- 전투/탐험/직업/장비/유물/저장 등 기능이 여러 JS 파일로 분리되어 있다.
- 역병숙주(rogue_alchemist) 전용 에픽 각인 3종(`re_venomrush`/`re_venomburst`/`re_solovenom`)은
  이미 새 "기생형" 메커니즘(`enemy.venomStacks`, `getVenomDmgPerStack()`)에 맞춰
  재연결되어 있었다 — 예전 문서(jobs.js 코멘트)에 "미해결"이라고 적혀 있던 건
  낡은 정보였음(실제 코드가 최신).

## 최근 작업 (이번 세션 — 굴복/도감/UI통일/시간의 파수꾼/층별보스 재설계)

> 직전 세션(마녀 재도전 시스템)은 `HANDOFF.md` 최상단으로 이동했다.

### 1. 굴복 시스템 + 첫 층별보스 고정 버그 수정
- `combat/player-actions.js`: `playerSurrender()` 신설 — 보통/하드코어 전용,
  확률 없이 항상 성공, 소지 골드의 30%/60%/85%(누적 사용 횟수 기준)를 대가로
  지불. 도망 버튼 자리를 난이도에 따라 도망(쉬움)/굴복(보통·하드코어)으로 전환
- `combat/battle-setup.js`: `pendingRematchSpec` 신설 — 굴복 시 상대 스펙(타입+
  정예 특성)을 저장해두고 재도전 시 무작위 재롤 대신 그대로(항상 풀피로)
  재구성. 첫 층별보스 고정 로직도 처음엔 `depth===5`로 잘못 넣어서(실제
  보스 사이클은 10층 단위) 한 번도 발동 안 하고 있었음 — `depth===10`으로
  수정

### 2. 몬스터 도감 정비
- 카운트가 "33/20"처럼 분모를 넘던 버그 수정 — 최종보스류 타입까지 도감에
  섞여 저장되고 있었음. 표시 시점에 유효 타입만 필터링해 자연 복구되게 함
- 층별보스 8종을 도감에 포함(조우 시점 등록), 이후 최종보스/진최종보스도
  포함하되 **처치 시점에만** 등록되도록 분리(`combat/battle-end.js`)
- `index.html`/`explore.js`: 상태바에 HP/MP 아래 EXP 바 추가

### 3. 하드코어 묘비 이벤트
- 하드코어 사망 시 `player.hardcoreGrave`에 스냅샷 저장(캐릭터당 최대 1개,
  "가장 깊이 도달했던 죽음" 기준으로만 갱신). 같은 구간을 다시 클리어해
  마을에 도착하면 사망 당시 장비/유물 중 최대 3개를 무작위 제시해 하나만
  회수 가능(1회성). 후보가 하나도 안 남으면 안내 로그만 남김
- 하드코어 사망 시 장비 인벤토리(`equipOwned`)가 유물과 함께 부활하던
  버그도 같이 수정(이제 정말 유물만 남음)

### 4. UI 통일 — 사기꾼 도박판 / 정예 특성 경고
- "조작된 도박판" 토스트를 텍스트 나열 대신 좌우 박스(초록/빨강)로 단순화
- 정예 특성 경고(광기/광폭화/사냥꾼)를 토스트 대신 보스 예고 카드
  (`updateBossIntentCard()`)로 통일 — 조건이 풀리면 자동으로 사라짐(예전
  토스트는 한 번 뜨고 끝이라 진행 상태를 알 수 없었음)

### 5. 시간의 파수꾼 — 신규 중간보스
- 보통/하드코어 3번째 구간(`tierIndex===2`) 노드맵 중간에 강제 수렴 'midboss'
  노드 신설(지도엔 "???"로 표시, 보라색 펄스 스타일). `combat/battle-setup.js`의
  `TIME_GUARDIAN` 데이터, `nodemap.js`의 `nodeMidboss` 플래그로 구현
- 핵심 기믹 "메아리": 결빙의 궤적(3턴 쿨, 속도 2턴 감소)/기본 공격이 2턴 뒤
  60% 위력으로 재발동(최대 1개 대기). 시간 역행(HP50% 이하 1회, 예고 없이
  즉시 발동). 초기 구현에 "매 턴 무조건 큐를 덮어써서 메아리가 사실상 한
  번도 안 터지는" 버그가 있었음 — "큐가 비었을 때만 새로 쌓기"로 수정
- 결빙의 궤적 전용 배너 + 화면 섬광 이펙트(`.frost-flash-fx`, 순수 CSS)
- 사용자 제공 컨셉 아트 적용(캐릭터/전용 배경)
- 버그 수정 2건: (a) 전투 중 새로고침하면 노드가 "방문 완료" 처리된 채
  전투 없이 다음 노드로 넘어갈 수 있었음 — boss/midboss 한정 안전장치를
  combat/elite 노드까지 확장(물음표 이벤트발 전투는 범위 밖, 알려진 한계)

### 6. 층별보스 5종 고정 배치(A안) + 4종 디자인/서사 전면 교체
- `combat/battle-setup.js`의 `FLOOR_BOSS_BY_DEPTH`로 무작위 풀 대신 고정:
  10=감시자의 석판 / 20=넝쿨진 칼날꽃(정원사) / 30=열쇠 두른 파수꾼(문지기,
  왕자 서브플롯 연결) / 40=고쳐지지 않는 시계(시계공) / 50=빈 옷의 예언자
  (별자리). 재봉인형/등롱/모래 3종은 `data/monsters.js`/`monster-visuals.js`
  에 소스만 남기고 주석 처리(A안 — 삭제 아님)
- 예언자는 depth50 자리에 맞게 스탯 상향(원래 초반용 시드값이라 그대로면
  depth40 고정 보스보다 약해지는 역전 현상이 있었음)
- 디자인 철학 전환: "무기/오브젝트를 든 사람"이 아니라 "사물이 사람의
  잔재를 흉내 낸다"는 방향으로 4종 전부 재설계(이미지 교체 완료)

### 7. 그 외 텍스트 다듬기
- 난이도 설명글을 시스템 설명 대신 서사적 한 줄로 교체
- 6개 직업 설명글에서 스탯 부연 문장 제거, 정체성 한 문장만 남김

### 커밋
`e0ed852`~`1a8d30b` 구간 전부(약 20개 커밋) — 전부 main에 푸시 완료.

## 최근 검증 상태

- 위 1~7 전부 `node --check` 통과.
- **미검증**(브라우저 실전 확인 전부 안 됨):
  - 굴복 비용 체증, 재도전 시 동일 몬스터(정예 특성 포함) 재현
  - 하드코어 묘비 전체 흐름(사망→재도전→같은 구간 클리어)
  - 시간의 파수꾼 전체 흐름 — 메아리 발동 타이밍, 노드 강제 수렴, 이미지/
    배경 표시, 새로고침 후 정상 재개
  - 층별보스 고정 배치 — 각 층에서 지정한 보스가 실제로 뜨는지, 새 이미지
    4종 정상 표시, 예언자(depth50) 체감 밸런스
  - EXP 바, 도박판/정예 경고 UI 실제 렌더

## 완료된 것으로 취급할 수 있는 영역

프로젝트에는 다음 핵심 시스템이 이미 존재한다.

- 기본 직업 시스템 / 2차 전직 14종(밸런스 시뮬레이션 완료, 조정은 미착수)
- 턴제 1:1 전투 / 몬스터·보스(층별보스 5종 고정 배치, 시간의 파수꾼 중간보스,
  일반 최종보스, 진 최종보스 2종)
- 스킬 / 장비 / 유물(정체된 맹세/결투자의 서약 구현 완료, 질주의 서약/무일푼의 각오는 개발 안 함으로 확정) / 에픽 직업 각인 시스템(42개, 역병숙주 +3)
- 탐험(굴복 시스템, 하드코어 묘비 이벤트 포함) / 상점 / 대장간 / 저장·불러오기 /
  기록·유물 도감·몬스터 도감(층별보스/최종보스류 포함, 36종)·업적·상태창
- 실제 음원 기반 BGM(6모드) + SFX(9종, 하이브리드) / 몬스터 시각화
- 세계관 스토리 레이어(오프닝 서사, 지역 묘사, 물음표 이벤트 32개+조건부 3개,
  특수 조우 대사 다수, 아이온/시조 진최종보스 2종, 왕자/아코스 떡밥 회수 진행 중)
- 마녀(아이온) 재도전 시스템: 조우 조건(시조 클리어 기록 필요), 난이도별
  배지(왕관/마녀모자 분리), 문지기 시계 제안 이벤트, 마녀 2회차 전용 대화
  엔딩(하드코어 추가 대사 포함), 보통/하드코어 시작 유물 제단 — 문지기=아이온
  떡밥은 이 시스템으로 대부분 회수됨(story.md 9장 참고)
- **시간의 파수꾼(신규, 브라우저 미검증)**: 보통/하드코어 3구간 중간보스,
  메아리(2턴 지연 재발동) 기믹, 전용 이펙트/배경
- **층별보스 재설계(신규, 브라우저 미검증)**: 5종 고정 배치 + 4종 디자인/
  서사 전면 교체(재봉인형/등롱/모래 3종은 소스만 보존, 미사용)
- UI 타이포그래피/버튼 리디자인(한글 정식 지원 폰트, 평면 버튼), EXP 바,
  보스 예고 카드로 통일된 정예 특성 경고

단, "완전히 테스트 완료"라는 의미는 아니다. 실제 테스트 여부는 해당 작업의 검증 결과를 따른다.

## 작업 시 주의

### 전투
전투 파일들은 서로 강하게 연결되어 있다. 작은 변경도 호출 순서와 턴 전환에 영향을 줄 수 있다. `player-actions.js`의 phys/magic 공용 대형 핸들러는 특례 분기가 얽혀있어 편집 후 반드시 재확인한다.

### 저장
저장 필드 변경은 `player.js`와 `storage.js`를 함께 확인한다. 단, `storage.js`가 `player` 객체를 통째로 직렬화하는 구조라(`JSON.stringify({player, ...})`), `player.xxx = player.xxx||기본값` 패턴으로 쓰는 새 필드는 두 파일을 안 건드려도 안전하다. 유물/몬스터 도감처럼 "계정 단위 영구 기록"은 `player` 세이브와 별개 키(`localStorage`/`window.storage`)로 관리한다(`RELICDEX_KEY`/`MONSTERDEX_KEY` 패턴 재사용).

### 사운드
`sound.js`가 BGM/SFX 둘 다 관장한다. BGM은 `<audio>` 엘리먼트, SFX는 `AudioBufferSourceNode` — 서로 다른 재생 메커니즘이니 혼동하지 않는다.

### UI/타이포그래피
제목류는 `var(--font-title)`(Gowun Batang), 그 외 전부는 `var(--font-ui)`(IBM Plex Sans KR). 색상 토큰은 `:root`의 기존 변수를 재사용한다.

### 직업
직업/전직 변경은 `jobs.js`, `job-advancement.js`, 필요 시 `skills.js`, `battle-end.js`를 함께 확인한다.

### 스토리/서사
`origin.js`(오프닝), `events.js`(물음표 이벤트), `combat/battle-setup.js`(특수 조우 대사·진최종보스 데이터), `combat/enemy-turn.js`(보스 스킬 실행), `combat/battle-end.js`(엔딩)에 서사 레이어가 분산돼 있다. 새 서사 요소 추가 시 이 파일들이 기본 확인 대상. `story.md`를 함께 갱신한다.

### 몬스터
데이터/전투 생성/시각화가 분리되어 있으므로 작업 성격에 맞는 파일만 선택한다. `data/monsters.js`의 `dex` 필드에 도감용 플레이버 텍스트가 있다 — 새 몬스터 추가 시 이 필드도 함께 채운다. 층별보스는 `combat/battle-setup.js`의 `FLOOR_BOSS_BY_DEPTH`로 depth별 완전 고정(무작위 아님) — 새 층별보스 추가/교체 시 이 맵과 `data/monsters.js`의 BOSSES 항목을 함께 확인한다.

## 다음 작업

```text
### 1순위 — 이번 세션 신규 콘텐츠 브라우저 실전 검증
- 위 "최근 검증 상태" 미검증 항목 전부(특히 시간의 파수꾼/층별보스 재설계 —
  가장 최근이고 변경 범위도 큼)

### 2순위 — 남은 정리 항목
- 층별보스 정리(A안) 후 자잘한 leftover: `combat/battle-setup.js`의
  `isFloatingBoss` 판정 배열에 퇴역한 재봉인형/등롱 이름이 남아있음(연출
  분기용, 동작엔 영향 없음 — 신경 쓰이면 정리)
- 물음표 이벤트 내부에서 전투로 이어지는 경우(예: "도발한다")는 새로고침
  스킵 방지 로직 범위 밖 — 필요하면 별도 보완

### 3순위 — 보통/하드코어 재미 요소 후속 (아이디어 단계, 미착수)
- 유물 조합 시너지 4종(A 회랑의 모든 유산/B 네 개의 도박/C 계약자/D 축복받은
  자) — 숫자 보상 없이 히든 업적+토스트로 가는 방향까지 합의, 구현 전
- 서약 선택 시스템(보통/하드코어에서 저주를 자원해서 짊어지고 확정 보상을
  받는 안) — 아이디어 단계, 수치/UI 미확정

### 4순위 — 2차 전직 밸런스 조정
- 황금 도박사: 에픽셋 연결/연패 완화/각인 완화는 완료 — 브라우저 실전 검증
  필요. 판돈 스노우볼 자체(성공 시 판돈 2배 재투자)는 아직 미조정
- 사기꾼(데미지 액티브 부재)은 아직 미착수

### 5순위 — 역병숙주 구조적 한계 재검토
- checkBattleEnd() 타이밍 이슈로 인한 독틱 증발 문제(전투 체인을 건드려야 해서 리스크 큼)

### 6순위 — 기타 후순위
- 남은 SFX 슬롯(guard/poisonHit/fail/click/victory/gameOver/statusApply) 음원 확보 여부 확인
- 감시자의 석판 디자인 교체 여부(스토리는 이미 확정 — "태초부터 있던 서약의
  석판". 디자인은 그대로 두기로 했었으나 재고 가능)
```

> 질주의 서약 / 무일푼의 각오(계약형 유물 2종)는 사용자 요청으로 할일 목록에서
> 완전히 제외됨 — 더 이상 개발하지 않는다.

## 상태 업데이트 규칙

작업이 끝나면:

1. 완료된 작업을 추가한다.
2. 변경 파일을 기록한다.
3. 미검증 항목을 기록한다.
4. 다음 작업을 1~5개 이내로 적는다.
5. 오래된 완료 항목은 필요하면 별도 HANDOFF/CHANGELOG로 이동한다.

이 문서는 길게 만들지 않는다. 현재 상태만 유지한다.
