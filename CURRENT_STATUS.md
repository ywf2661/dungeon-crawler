# CURRENT_STATUS.md — 현재 개발 상태

> 이 문서는 과거 대화의 대체물이다.
> 새 Claude 대화를 시작할 때 현재 상태를 빠르게 전달하기 위한 문서다.
> 오래된 상세 작업 이력은 `HANDOFF.md`를 참고한다.

## 기준 브랜치

- `main` (이번 작업은 `mechanic-stoker-accumulator` 브랜치에서 진행)

## 현재 확인된 상태

- 프로젝트는 Vanilla JS/HTML/CSS 기반의 정적 브라우저 게임이다.
- 여러 classic `<script>`가 로드 순서에 따라 공유 전역 scope를 사용하는 구조다.
- 전투/탐험/직업/장비/유물/저장 등 기능이 여러 JS 파일로 분리되어 있다.
- `js/`에는 기존 문서에 적힌 파일 외에도 `events.js`, `nodemap.js`, `origin.js`가 존재한다.
- 기존 `PROJECT.md`는 일부 최신 파일/구조보다 뒤처져 있으므로 실제 코드를 우선한다.

## 현재 상태 요약

최근 작업들은 크게 두 갈래다 — 상세 내역은 전부 `HANDOFF.md`에 있다.

1. **에픽 직업 각인 시스템**(39개 = 13개 특성 × 3개) 데이터+전투 로직 전부
   완전 구현 완료. 대장간 강화 후보에 특성 인식형으로 섞여 나오며, 직업
   각인은 보라색 테두리+"✨" 배지로 시각 구분된다. 39개 전체를 "적용 전/후
   피해 변화율" 방식으로 비교 검증해 삼위일체 각인(마법사 계약술사)의
   과도한 수치를 하향 조정했다.
2. **기관사 2차 전직 리뉴얼**(폭주 화부/강철 군단장), **광대 2차 전직
   리뉴얼**(황금 도박사/사기꾼), **순교자 레벨12/15 신규 스킬**, **상태창
   신규 구현**, 그 외 다수의 밸런스 수정과 버그 수정(공격력 버프가 마력
   기반 직업엔 안 먹히던 문제, "다음 전투 한정" 버프가 실제로는 여러 전투
   지속되던 문제 등).

## 최근 작업

- **독사(rogue_alchemist) 독 스택당 라운드 피해 비율 0.09→0.13 상향** (커밋
  `75bc6e7`). 여러 턴짜리 완전한 전투 사이클 기준 Monte Carlo(20,000회)로
  독사를 다른 2차 전직(순일격의 구도자/저주술사)과 비교한 결과, 보스전 기준
  약 20~25% 낮은 화력으로 확인됨. 원인 분석: "죽지 않는 더미" 기준 순수
  화력 자체는 낮지 않았음(오히려 근소 우위) — 진짜 원인은 독사 특유의
  구조적 손실. 직접타로 적을 처치하면 `checkBattleEnd()`가 즉시 발동해 그
  턴 쌓여있던 독틱 피해가 통째로 증발한다(전투 체인상 어쩔 수 없음, 고치려면
  `checkBattleEnd()` 호출 시점을 바꿔야 해서 이번엔 손대지 않음). 전투 흐름은
  그대로 두고 기본 비율만 올려 격차를 메움. 참고: 이 비율은 과거 세션에서
  "독사가 너무 강하다"는 정반대 이유로 0.18→0.09로 절반 삭감됐던 값 —
  0.18로 완전히 되돌리지 않고 절충값 0.13 선택.
  - 변경 파일: `enemy-turn.js`(getVenomDmgPerStack 상수)
  - 검증: 보스전 평균 처치 턴수 5.00→4.00 (Monte Carlo 20,000회), 일반/정예
    몬스터전은 원래도 2~3턴 안에 끝나 영향 없음
  - 미검증: 실제 브라우저 플레이 테스트
- **독사(rogue_alchemist) 고독 각인(re_solovenom) 밸런스 재설계** (커밋
  `26f5472`, `main`/`mechanic-stoker-accumulator` 양쪽 반영). 기존엔 스택
  상한만 10→7로 줄이고 보상이 없어, 피해 공식(스택수×스택당피해, 선형)
  특성상 사실상 죽은 각인이었다(Monte Carlo 20,000회로 확인). 상한 감소폭을
  10→6으로 완화하고 스택당 매 라운드 피해 +25%를 추가해 보상했다
  (`enemy-turn.js`의 `getVenomDmgPerStack()`). 재검증 결과 보스전 기준
  평균 생존HP 51.4%→58.6%, 평균 턴수 5.00→4.41로 개선됨을 확인.
  - 변경 파일: `blacksmith.js`(desc), `player-actions.js`(venomCap),
    `enemy-turn.js`(getVenomDmgPerStack)
  - 미검증: 실제 브라우저 플레이 테스트
- **독사 범용 독중첩 훅 3곳의 상한 무시 버그 수정** (커밋 `982d9ba`).
  `getVenomStackCap()` 헬퍼로 통합해 기본 공격/다타 스킬/범용 phys·magic
  훅까지 총 4곳이 전부 같은 상한(고독 각인 시 6, 아니면 10)을 참조하도록
  통일. 아래 "알려진 설계 단순화"의 해당 항목은 이제 해소됨.

## 최근 검증 상태

- 문법 검사(`node --check`)는 매 작업마다 통과했지만, **이번 세션 전체가
  브라우저 실전 테스트를 단 한 번도 거치지 않았다.** 다음 세션의 최우선
  순위는 실전 플레이를 통한 버그 발견이다.
- 알려진 설계 단순화(완벽 재현 대신 안전한 방식으로 대체한 것들):
  - 도적 "겹저주 각인": 진짜 도트 2중첩 대신 "재시전 시 미리 30% 터뜨리기"
  - 사기꾼 "이중 손버릇 각인": 방어력을 진짜 0으로 만드는 대신 받는 피해 3배로 근사
  - 성기사 각인들의 "최대HP 30%+ 피해" 판정이 완전히 최종 확정된 데미지가 아니라 일부 배율 적용 전 값 기준(오차 적을 것으로 예상)
- **독사의 구조적 한계(설계 결함, 미해결)**: 독을 걸어둔 적을 직접타로
  처치하면 `checkBattleEnd()`가 즉시 발동해 그 턴 쌓여있던 독틱 피해가
  통째로 증발한다. 이번 세션에 비율 상향(0.09→0.13)으로 숫자상 격차는
  메웠지만, 이 근본 손실 자체는 그대로 남아있다. 고치려면 `checkBattleEnd()`
  호출 시점이나 전투 체인 순서를 바꿔야 해서 리스크가 크다(전투 관련 특별
  규칙 위반) — 건드릴 거면 별도로 신중하게 설계부터 논의 필요.
- 39개 각인 밸런스는 "시전 1회 기준 배율 비교"로만 검증했다(완전한 Monte
  Carlo 아님). 삼위일체 외 이상치는 안 보였지만 실전 확인은 필요하다.
  단, 독사 고독 각인은 이번에 여러 턴에 걸친 전체 전투 사이클 기준
  Monte Carlo(20,000회)로 개별 재검증했다.

## 완료된 것으로 취급할 수 있는 영역

프로젝트에는 다음 핵심 시스템이 이미 존재한다.

- 기본 직업 시스템 / 하이브리드 2차 전직(전 직업 완성, 순교자 레벨12/15 포함)
- 턴제 1:1 전투 / 몬스터·보스(층별보스, 일반/진 최종보스)
- 스킬(1차/2차 각성기 포함) / 장비 / 유물
- **에픽 직업 각인 시스템**(39개, 대장간 강화 후보에 특성 인식형으로 통합)
- 탐험 / 상점 / 대장간 / 저장·불러오기 / 기록·도감
- 상태창(능력치 자연치+보너스 표시, 스킬 목록)
- Web Audio 기반 사운드 / 몬스터 시각화 / 이벤트·노드·기원 관련 코드

단, "완전히 테스트 완료"라는 의미는 아니다. 실제 테스트 여부는 해당 작업의 검증 결과를 따른다.

## 작업 시 주의

### 전투
전투 파일들은 서로 강하게 연결되어 있다. 작은 변경도 호출 순서와 턴 전환에 영향을 줄 수 있다. 특히 `player-actions.js`의 phys/magic 공용 대형 핸들러는 여러 특례 분기가 얽혀있어, 근처를 편집할 땐 str_replace 후 반드시 재확인한다(이번 세션에 실수로 코드를 지울 뻔한 적이 여러 번 있었음).

### 저장
저장 필드 변경은 `player.js`와 `storage.js`를 함께 확인한다.

### 직업
직업/전직 변경은 `jobs.js`, `job-advancement.js`, 필요 시 `skills.js`, `battle-end.js`를 함께 확인한다.

### 스킬
스킬 데이터와 실행 로직이 분리되어 있으므로 `data/skills.js`만 수정하고 끝내지 않는다.

### 몬스터
데이터/전투 생성/시각화가 분리되어 있으므로 작업 성격에 맞는 파일만 선택한다.

### 에픽 직업 각인
`blacksmith.js`의 `EPIC_JOB_ENCHANTS`(→`ENHANCEMENTS`에 병합)가 데이터 원본이다. `specId`로 특성 필터링, `exclusiveGroup`으로 택1류 상호배제를 처리한다. 새 각인 추가 시 데이터 등록만으로 끝나지 않고 실제 전투 로직(주로 `player-actions.js`/`enemy-turn.js`)까지 반드시 연결해야 한다 — 데이터만 있고 로직이 없으면 골라도 아무 효과가 없다.

## 다음 작업

```text
### 1순위 — 브라우저 실전 테스트
- 목표: 이번 세션 전체(직업 리워크 2개, 순교자 신규 스킬, 각인 39개,
  밸런스/버그 수정 10여 건)를 실제로 플레이해 크래시·오작동 확인
- 특히 확인할 것: 회랑의 기사 방어구 택1이 실제로 상호 배제되는지,
  사기꾼 손버릇 재시도 체인(이중 손버릇 3연속)이 무한루프 없이 도는지,
  강철 군단장 로봇 슬롯/오메가 후광 UI가 안 겹치는지, 상태창이 정상 표시되는지
- 완료 조건: 각 직업 최소 1회씩 전투~레벨업~전직까지 플레이해 크래시 없음 확인

### 2순위 — 39개 각인 정밀 밸런스 검증
- 목표: 지금까지는 "시전 1회 배율 비교"만 했음 — 필요하면 Python Monte
  Carlo로 각 특성의 전체 전투 사이클(여러 턴) 기준까지 검증
- 완료 조건: 특성별로 각인 유/무 딜 차이가 다른 특성들과 비슷한 수준인지 확인

### 3순위 — 알려진 설계 단순화 재검토
- "최근 검증 상태"에 나열된 단순화 항목들(겹저주/이중손버릇/고독 각인 등)을
  원래 기획대로 더 정교하게 구현할지 결정
```

## 상태 업데이트 규칙

작업이 끝나면:

1. 완료된 작업을 추가한다.
2. 변경 파일을 기록한다.
3. 미검증 항목을 기록한다.
4. 다음 작업을 1~5개 이내로 적는다.
5. 오래된 완료 항목은 필요하면 별도 HANDOFF/CHANGELOG로 이동한다.

이 문서는 길게 만들지 않는다. 현재 상태만 유지한다.
