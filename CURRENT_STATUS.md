# CURRENT_STATUS.md — 현재 개발 상태

> 이 문서는 과거 대화의 대체물이다.
> 새 Claude 대화를 시작할 때 현재 상태를 빠르게 전달하기 위한 문서다.
> 오래된 상세 작업 이력은 `HANDOFF.md`를 참고한다.

## 기준 브랜치

- `main` (이번 작업은 `mechanic-stoker-accumulator` 브랜치에서 진행)

## 현재 확인된 상태

- 프로젝트는 Vanilla JS/HTML/CSS 기반의 정적 브라우저 게임이다.
- 여러 classic `<script>`가 로드 순서에 따라 공유 전역 scope를 사용하는 구조다.
- 전투/탐험/직업/장비/유물/저장 등 기능이 여러 JS 파일로 분리되어 있다.
- `js/`에는 기존 문서에 적힌 파일 외에도 `events.js`, `nodemap.js`, `origin.js`가 존재한다.
- 기존 `PROJECT.md`는 일부 최신 파일/구조보다 뒤처져 있으므로 실제 코드를 우선한다.

## 최근 작업

### 버그 수정 — 버프 배지가 "99턴"으로 표시되던 문제
- 원인: 휴식 노드 명상 등 장기 지속형 버프는 관례적으로 `buffAtkTurns=99`
  (큰 숫자, 실제로는 라운드당 1씩만 깎여 여러 전투에 걸쳐 유지)로 표현하는
  기존 패턴인데, 지난 세션에 추가한 버프 배지가 이 숫자를 그대로 "99턴"
  이라고 찍어버렸음(버그 아님, 표시만 문제).
- `battle-fx.js`에서 90 이상이면 턴 수 대신 "지속 중"으로 표시하도록 수정
  (공격력 버프/방어 버프 배지 둘 다).
- `node --check` 전체 통과.

### 일반 최종보스("잠식된 OO 용사") — 무결 클리어 기록 제외
- 기존엔 가장 최근 기록을 무조건 가져다 썼는데, 그 기록이 무결 클리어
  (진엔딩+무사망)였다면 제외하도록 수정(사용자 요청) — 한 번도 무릎
  꿇지 않은 자가 타락한 모습으로 나타나는 건 서사적으로 안 맞음.
- `explore.js`에서 기록 목록을 `!(trueEnding && deathCount===0)`로 필터링한
  뒤 가장 최근 것을 사용, 전부 무결이면 기존처럼 무작위 직업으로 폴백.
- `node --check` 전체 통과.

### 대장간 강화 목록 — 직업 각인 시각 구분 추가
- 강화 후보 선택 화면에서 직업 각인(specId 있는 것)에 보라색 테두리 +
  "✨ 직업 각인" 배지를 붙여 일반 강화와 확실히 구분되게 함(사용자 요청).
- 이미 새겨진 강화 목록(대장간 홈 화면)에도 직업 각인은 ✦ 대신 보라색
  ✨ 마크로 구분.
- `node --check` 전체 통과.

### admin3 계정 수정 — 사전 강화 제거
- 기존엔 에픽 장비에 딜 위주 강화(sharp/heavy/brutal 등)가 미리 새겨진
  채로 지급됐는데, 사용자 요청으로 전부 제거 — 이제 무강화 상태로
  지급/장착되며, 대장간에서 강화석(100개 보유)으로 원하는 강화나 새
  직업 각인을 직접 골라 테스트할 수 있다.
- `node --check` 전체 통과.

### admin3 계정 수정 — 마을 시작 + 강화석 100개
- 기존엔 `town=false`로 시작해 곧바로 "고요한 제단" 노드맵 한가운데(진
  최종보스 직전 노드)에 떨어졌는데, 사용자 요청으로 `town=true`로 바꿔
  **마을에서 시작**하도록 변경. `player.nodeMap`은 그대로 미리 만들어둬서,
  `explore.js`의 `onAdvance()`가 이미 갖고 있는 "마을에서 나아갈 때 지도가
  있으면 그대로 재사용" 로직을 그대로 타게 만들었다(별도 분기 불필요) —
  마을에서 "나아가기"를 누르는 순간 곧바로 진 최종보스 직전 구간으로 들어감.
- `player.reinforceStones = 100` 추가(에픽 각인 테스트용).
- `node --check` 전체 통과. 미검증: 브라우저 실전 확인 안 함.

### 도박사 에픽 각인 6종(황금 도박사/사기꾼) 구현 — 전체 39개 각인 완료
- **황금 도박사**: 몰빵 각인(무기 — 판돈상한1.5배, 실패시 이후 골드획득 반토막),
  사기 정보 각인(방어구 — 정보료 보너스/비용 2배), 빚투 각인(장신구 — 올인
  한정 골드150%까지 베팅, 실패시 최대HP15% 고정피해로 안전하게 구현).
- **사기꾼**: 겹패 각인(무기 — 운명뒤바꾸기 `fateSwapUsed` 불리언을 카운터로
  전환해 1→2회), 이중 손버릇 각인(방어구 — `checkGamblerRetry(key,isRetry)`
  로 시그니처 확장해 재시도 실패시 1회 더, 최종 실패시 받는피해 3배),
  속임수 폭로 각인(장신구 — 주사위 항상6, 이후 2턴 MP소모+20%, enemy-turn.js
  라운드 카운트다운에 페널티 소진 로직 추가).
- **전체 39개 각인(13개 특성 × 3개) 데이터+전투로직 완전 구현 완료** —
  전사6/마법사9/도적6/성기사6/기관사6/도박사6.
- `node --check` 전체 통과. 미검증: 브라우저 실전 테스트 전혀 안 함 —
  39개 각인 전부 실전 확인이 필요한 상태.
- 알려진 설계 단순화 2건: 도적 "겹저주 각인"은 진짜 2중첩 대신 "재시전시
  미리 30% 터뜨리기"로 대체, "이중 손버릇 각인" 3연속 실패 페널티는
  방어력을 0으로 만들지 않고 받는피해 3배로 근사.

### 성기사 에픽 각인 6종(순교자/회랑의 기사) 전투 로직 완전 구현
- **순교자**: 즉각 순교 각인(심판의빛 — 희생 발동시 피해+50%/안하면 -10%),
  가시 갑옷 각인(순교자의 인장 방어보너스 유지+누적횟수 비례 반사딜),
  영겁 회귀의 각인(불멸의 순교 — "처치시"를 "최대HP30%+피해"로 수정해 반동/
  쿨다운 복구).
- **회랑의 기사**(무기 슬롯 없음 — 칼리버X 강제장착이라 애초에 강화 불가):
  방어구 택1(exclusiveGroup:'knight_armor' — 이 세션 초반에 만들어둔 인프라
  실전 사용 첫 사례) 순교자의 서약(검은기도 공+80%·3턴/방어-55%) vs 성좌의
  가호(성휘참 피해-10%/흡혈2배+2턴 피해감소 배리어), 장신구 종언을 넘어서
  (칼리버X:종언 — 마찬가지로 "최대HP30%+피해"로 수정).
- 누적: 전사6+마법사9+도적6+성기사6 = **27개 각인 전투 로직 완전 구현**.
- `node --check` 전체 통과. 미검증: 브라우저 실전 확인 안 함(특히 방어구
  택1 exclusiveGroup이 실제로 후보 필터링에서 잘 작동하는지).
- **남은 것**: 기관사 2특성 6종, 도박사 2특성 6종.

### 도적 에픽 각인 6종(환영검사/독사) 전투 로직 완전 구현
- **환영검사**: 이중 쇄도 각인(그림자쇄도 시 분신배가 자동적용+즉발피해
  -15%), 잔상 각인(분신배가 1회 연장 — `consumeDoubleImageArmed()` 헬퍼로
  두 소비 지점 통합), 불멸 군단 각인(백귀야행 잔영누적 유지, 쿨다운 3→5턴).
- **독사**: 폭주 주입 각인(자기보너스 2배, 상한초과시 반동피해), 폭발
  정제 각인(스택 최대시 자동폭발+리셋, `enemy-turn.js` 라운드 틱 지점에서
  분기, 정제보너스 30%→15%), 고독 각인(보너스 2배 중첩 가능, 상한 10→7).
- 누적: 전사6 + 마법사9 + 도적6 = **21개 각인 전투 로직 완전 구현**.
- `node --check` 전체 통과. 미검증: 브라우저 실전 확인 안 함. 알려진
  경미한 한계: 고독 각인(상한7)을 껴도 기본공격 등 범용 독중첩 훅은
  여전히 하드코딩된 상한10을 참조해 이론상 7을 넘길 수 있음(사용 빈도
  낮은 엣지케이스라 이번엔 방치).
- **남은 것**: 성기사 2특성 6종(회랑의 기사 택1 처리 포함), 기관사 2특성
  6종, 도박사 2특성 6종.

### 마법사 저주술사 에픽 각인 3종 전투 로직 완전 구현
- **임시 계약 각인**(무기): 전투당 1회, 저주 스킬 중 먼저 쓰는 것이 저주
  1개를 더 짊어진 것처럼 계산. `relics.js`에 `getCombatCurseCount()` 신설
  (전투 데미지 계산 전용 — 원본 `getCurseCount()`를 직접 바꾸면 보상 배율
  등 무관한 시스템까지 같이 부풀려져서 별도 함수로 분리), 저주 폭발/저주
  각인/저주 만개 3곳 모두 이 함수로 교체.
- **겹저주 각인**(방어구): 원래 설계(도트 2중첩)는 기존 dot 시스템이 "같은
  타입은 덮어쓰기"만 지원해 위험 부담이 있어, **"재시전 시 남은 도트를
  미리 30% 터뜨린 뒤 새로 건다(신규 도트는 20% 약화)"**는 더 단순하고
  안전한 방식으로 대체 구현(설계와 다르게 단순화됨 — 참고).
- **만개 순환 각인**(장신구): 저주 만개가 원래 흡수·소멸시키는 저주 각인의
  잔여 도트를 그대로 살려둔 채 발동. 대신 만개의 저주 개수 보너스 -20%.
- 누적: 전사 6 + 마법사(시간술사 1/계약술사 3/저주술사 3) = **13개 각인
  전투 로직 완전 구현**.
- **발견된 불일치**: 시간술사는 원래 3슬롯(무기 무한가속/방어구 역행/
  장신구 시간정지) 기획이었는데 방어구(역행의 각인) 1개만 구현됨 — 무기·
  장신구 2개는 다음 작업에서 마저 채워야 함.
- `node --check` 전체 통과. 미검증: 브라우저 실전 확인 안 함.
- **남은 것**: 시간술사 2종(무한가속/시간정지), 도적 2특성 6종, 성기사
  2특성 6종(회랑의 기사 택1 처리 포함), 기관사 2특성 6종, 도박사 2특성
  6종 — 데이터/로직 전부 아직.

### 마법사 계약술사 에픽 각인 3종 전투 로직 완전 구현
- **이중 계약 각인**(무기, 원소 각인/Lv10): 계약 원소 + 무작위 다른 원소
  하나를 동시에 터뜨림(각 70% 위력). 기존 4분기(화염/빙결/번개/미계약)
  코드는 안 건드리고, 이 각인 보유 시에만 완전히 별도 경로로 분기.
- **배신의 계약 각인**(방어구, 마스터리): 원소 계약 스킬(각인/파동/폭풍)
  3개가 공유하는 헬퍼 `checkPactBetrayal()` 신설 — 적중마다 20% 확률로
  계약 원소가 강제 전환되며 추가 폭발 피해.
- **삼위일체 각인**(장신구, 원소 폭풍/Lv15): 계약 원소 무관하게 화염+빙결+
  번개 전부 발동(각 스킬 온전한 위력), 대신 쿨다운 2배. 이것도 별도 경로.
- 이로써 전사 6종 + 마법사 시간술사 1종 + 계약술사 3종 = **10개 각인 전투
  로직 완전 구현 완료**.
- `node --check` 전체 통과. 미검증: 브라우저 실전 확인 안 함.
- **남은 것**: 마법사 저주술사 3종, 도적 2특성 6종, 성기사 2특성 6종
  (회랑의 기사 방어구 택1 exclusiveGroup 처리 포함), 기관사 2특성 6종,
  도박사 2특성 6종 — 전부 기획만 되어 있고 blacksmith.js 데이터/전투
  로직 모두 아직. "처치 시" 트리거 각인(회랑의 기사 "종언을 넘어서",
  순교자 "영겁 회귀의 각인")도 전사와 동일하게 "최대HP 30%+ 피해"로
  고쳐서 넣어야 함.

### 전사 에픽 각인 6종 전투 로직 완전 구현
- **중요한 설계 수정**: "처치 시 재발동" 컨셉 각인들(연쇄 처형/부활하는 각인
  등)이 이 게임의 1:1 전투 구조(적 처치=즉시 전투 종료, 다음 적 없음)와
  근본적으로 안 맞아서, 트리거 조건을 "처치 시"→**"적 최대HP의 30% 이상을
  이 한 방으로 깎으면"**으로 전부 수정(사용자 확정). 같은 문제가 있는
  회랑의 기사/순교자의 유사 각인도 다음에 이 기준으로 맞출 예정.
- **혈맹의 검투사**: 연쇄 처형(저돌 30%+피해 시 무료 재발동, 실패 시 방어력
  -20%), 불사의 광기(전투당 1회 HP1 생존, 이후 받는 피해 +25% — 불굴의
  갑옷과 동일 지점에 분기 추가), 부활하는 각인(혈옥쇄 30%+피해 시 HP소모/
  쿨다운 즉시 복구 — `hpSacActualCost`를 바깥 스코프로 끌어올려 재사용).
- **일격의 구도자**: 일섬의 각인(기본공격 확정 크리 2배, 명중률 70%),
  공명 각인(직전 기본공격 피해 20% 상속, `player.lastBasicAtkDmg`를
  playerSkill/playerItem 시작부에서 리셋), 무한 메아리 각인(쌍격의 파문
  확정 2타 이후 50%씩 재도전하는 재귀 함수 `tryInfiniteEcho`, 매번 위력
  -0.1배, 최저 0.1배).
- `battle-setup.js`에 `player.lastBasicAtkDmg` 전투 시작 시 초기화 추가.
- `node --check` 전체 통과. 미검증: 브라우저 실전 확인 안 함(특히 무한
  메아리 체인 연출, 일섬 미스 처리, 연쇄 처형 재귀 호출 흐름).
- **아직 데이터만 있고 로직 미연결**: 마법사 계약술사 3종(이중 계약/배신의
  계약/삼위일체) — 3원소 분기 코드가 복잡해 다음 작업으로 미룸.

### 에픽 직업 각인 시스템 — 인프라 구축 + 일부 구현 (진행 중, 대규모 작업)
- **완성된 인프라**: `blacksmith.js`에 `EPIC_JOB_ENCHANTS`를 기존 `ENHANCEMENTS`
  테이블에 병합(`Object.assign`)하는 방식으로 추가 — 그래야 표시/저장/장착
  관련 기존 함수를 하나도 안 건드리고 그대로 재사용됨. `renderBlacksmithChoice`
  의 후보 풀 필터링에 `specId`(이 특성으로 전직 + 에픽 등급일 때만 후보로
  노출) 및 `exclusiveGroup`(택1류, 회랑의 기사 방어구용 — 아직 데이터 없음)
  조건 추가.
- **데이터만 정의됨(전투 로직 미연결) — 골라도 아직 효과 없음**: 전사
  혈맹의 검투사 3종(연쇄 처형/불사의 광기/부활하는 각인), 전사 일격의
  구도자 3종(일섬의 각인/공명 각인/무한 메아리 각인), 마법사 계약술사 3종
  (이중 계약/배신의 계약/삼위일체).
- **완전히 구현됨**: 마법사 시간술사 "역행의 각인"(me_regression) — 시간
  왜곡 발동 확률 20%→35%(`enemy-turn.js`의 `getTimeWarpExtraChance()`),
  이 각인으로 발동한 추가 행동에서 가속 주문 위력 -20%
  (`battleFlags.timeRegressionActive` 플래그, `player-actions.js`의
  `haste` 타입 핸들러에서 1회 소모).
- 나머지 직업(마법사 저주술사/도적 2/성기사 2/기관사 2/도박사 2)은 기획만
  있고 데이터/로직 둘 다 아직. 전사·계약술사 데이터 미연결분도 다음 작업.
- **왜 여기서 멈췄나**: `저돌`(warriorBloodpactActive)의 HP비례 배율조차
  별도 공용 함수(`applySkillModifiers`)에 숨어있는 등, 건드려야 할 범위가
  기존 대형 공유 함수(`phys`/`magic` 타입 핸들러) 깊숙이 얽혀있어, 이번
  세션에서 이미 비슷한 자리 편집 중 실수로 코드 줄을 삭제할 뻔한 적이
  있었음(즉시 복구함) — 무리하게 더 밀어붙이기보다 검증된 만큼만 반영.
- `node --check` 전체 통과. 미검증: 브라우저 실전 확인 안 함(특히 역행의
  각인 실제 발동 확인 필요).

### 버프류 스킬 잔여 턴수 UI 표시 추가
- 검은 기도/총사령관의 명령뿐 아니라 warcry/paladinblessing 등 여러 스킬이
  공유하는 범용 버프 필드(`buffAtkTurns`/`buffDefTurns`/`buffCounterTurns`)까지
  전부 배지로 노출(사용자 요청 — "버프가 몇 턴 남았는지 모호하다"는 광범위한
  기존 문제였음).
- `combat/battle-fx.js`의 플레이어 상태 배지 렌더링부에 5종 배지 추가:
  공격력 버프/방어 버프/반격 태세(범용) + 검은 기도/총사령관의 명령(전용 필드).
- `node --check` 전체 통과. 미검증: 브라우저 실전 확인 안 함.

### 순교자(paladin_martyr) 레벨12/15 신규 스킬 구현
- 기존엔 순교자가 마스터리+레벨10(심판의 빛)만 있고 레벨12/15가 아예
  없었음(콘텐츠 누락 발견 — 각인 기획 도중 확인됨).
- **신규 필드** `player.martyrSacrificeCount`: 희생의 맹세(mastery_martyrvow)
  발동 때마다 1씩 증가(`player-actions.js`).
- **레벨12 패시브 "순교자의 인장"**(`paladinMartyrSeal`): 누적 희생 횟수
  1회당 방어력 +2(최대 10회분=+20). `enemy-turn.js`에 `getMartyrSealDefBonus()`
  신설, 피격 피해 계산(`mitigated = dmg - player.def - ...`)에 반영.
- **레벨15 궁극기 "불멸의 순교"**(`paladinMartyrUltimate`, 신규 타입
  `martyrultimate`): 누적 희생 횟수 비례 확정 피해(기본 2.0배+회당 +35%),
  사용 시 최대HP 15% 반동 소모. 반동으로 죽게 되면 전투당 1회
  (`battleFlags.martyrReviveUsed`) HP1로 생존. 쿨다운 3턴.
- `jobs.js`의 paladin_martyr에 `skillLevels: {12:'paladinMartyrSeal', 15:'paladinMartyrUltimate'}` 추가.
- `node --check` 전체 통과. 미검증: 실전 확인 안 함, 수치(방어력 +2/회,
  배율 2.0+0.35/회, 반동 15%)는 시뮬레이션 전 가안.

### 마법사/도적 2차각성 각인 설계 논의 (구현 안 함, 순수 브레인스토밍)
- 대장간 강화 슬롯 중 하나에 "직업 각인"이 후보로 섞여 나오는 방식으로
  합의(장비당 최대 1개, 새 슬롯 신설 아님). 캐릭터의 실제 전직 특성 기준
  으로 각인 후보가 갈린다(장비는 직업 단위 공유, 콘텐츠는 특성 인식형).
- 마법사(계약술사/시간술사/저주술사), 도적(환영검사/독사) 각인 초안까지
  논의 완료(코드 미반영, 순수 기획 단계). 성기사 각인 논의 중 순교자
  레벨12/15 누락을 발견해 스킬 구현이 먼저 필요했음(위 항목).
- 다음 논의 필요: 회랑의 기사(전용무기 강제장착으로 "무기 슬롯 각인"
  자체가 불가능한 구조적 문제, 미해결).

### 밸런스 수정 — 오메가 유닛(강철 군단장)이 화력 로봇보다 약했던 문제
- 오메가 4턴 합계 119 vs 화력 3턴 합계 132로, MP 2배+쿨다운3턴을 감수하는데도
  더 약했음(사용자 제보로 확인).
- `mechanicOverpressure`는 폭주 화부의 "과압 각성"과 완전히 공유되는 데이터라
  기존 `mult`/`rigMult`를 직접 올리면 폭주 화부까지 같이 세짐 — 그래서
  `legionBurstMult:1.5`/`legionRigMult:0.75`를 새로 추가하고, `player-actions.js`
  의 `isLegion` 분기에서만 이 값을 쓰도록 분리(폭주 화부는 기존 `mult:1.2`/
  `rigMult:0.4` 그대로, 영향 없음).
- 레벨17·mag46 기준: 오메가 합계 197(즉발61+틱34×4), 턴당평균49.2로 화력
  (132/44.0)을 확실히 상회하도록 조정.
- `node --check` 전체 통과. 미검증: 실전 확인 안 함.

### 버그 수정 — 난이도별 왕관(진 최종보스 무사망 클리어) 표시 안 되던 문제
- 원인 2가지 모두 확인:
  1. 엔딩→타이틀 복귀 시 저장되는 `record` 객체에애초에 `trueEnding` 필드가
     없었음(records.js의 뱃지 표시는 이 필드를 읽는데 한 번도 채워진 적 없음).
  2. `bootstrap.js`가 기록을 불러올 때 `normalUnlocked`/`hardcoreUnlocked`는
     계산하면서 정작 `easyFlawless`/`normalFlawless`/`hardcoreFlawless`(왕관
     표시용, `ui/difficulty.js`에 선언만 되고 대입되는 곳이 전혀 없었음)는
     계산 로직 자체가 없었음.
- 수정: `record`에 `trueEnding: !!player.trueEndingSeen` 추가, `bootstrap.js`
  기록 로드 두 지점 모두에 `record.difficulty`+`trueEnding`+`deathCount===0`
  기준으로 세 난이도 flawless 플래그 계산 추가.
- 주의: 기존에 이미 저장된 과거 기록에는 `trueEnding` 필드가 없어 왕관이
  소급 적용되지 않는다 — 이 수정 이후 새로 클리어한 기록부터 정상 표시됨.
- `node --check` 전체 통과. 미검증: 실전(무사망 진엔딩 클리어) 확인 안 함.

### 2차 각성기(레벨15) 전부에 쿨다운 3턴 통일 적용
- 기존엔 기관사 계열(폭주화부/강철군단장, 4턴)만 쿨다운이 있었고 나머지
  10개는 자체 HP비용/자원조건/1회제한 외엔 매턴 스팸 가능했음.
- 액티브 10개(혈옥쇄/원소폭풍/시간의역설/저주만개/백귀야행/칼리버X종언/
  임계폭주/총사령관의명령/올인/운명뒤바꾸기)에 `cooldown:3` 적용, 기존
  4턴이던 기관사 둘은 3턴으로 하향해 전부 통일.
- 패시브 2개(쌍격의 파문/삼중 주입 — 발동 순간이 없는 상시 효과)는 쿨다운
  개념 자체가 적용 안 돼서 제외.
- `node --check` 전체 통과. 미검증: 실전 확인 안 함(밸런스 영향 있을 수
  있음 — 특히 백귀야행/저주 만개처럼 스택형 궁극기는 매턴 못 쓰게 되는
  영향이 더 클 수 있어 추후 재확인 필요).

### 시작 장비 전 난이도 공통 지급으로 확장
- 이전엔 쉬움 난이도 전용이었던 시작 장비(기본 무기+가죽 갑옷)를 보통/
  하드코어에도 동일하게 지급하도록 변경(`player.js`의 `startEquip` 조건 제거).
- `node --check` 전체 통과.

### 쉬움 난이도 추가 혜택 — 골드 수급량 증가 + 시작 장비 지급
- **골드**: 몬스터 처치 골드(`battle-end.js` 주 보상식)와 층별보스 보상
  선택지("두둑한 보상")에 +25% 보너스.
- **시작 장비**: 쉬움 난이도로 시작하면 기본 무기+방어구를 처음부터 장착한
  채 시작(상점 최하급 아이템 재사용) — 물리 계열(전사/도적/성기사/도박사)은
  낡은 단검(w_dagger, atk+2), 마법 계열(마법사/기관사)은 견습생의 지팡이
  (w_staff, mag+5), 방어구는 공통으로 가죽 갑옷(a_leather, def+3/hp+8).
  `equipOwned`에도 들어가 있어 상점에서 팔거나 나중에 다른 장비로 교체 가능.
- `node --check` 전체 통과. 미검증: 실전 확인 안 함.

### 쉬움 난이도 접근 방식 전환 — 적 약화 대신 플레이어 강화
- enemy atk 배율을 너프 이전(1.0)으로 되돌림(hp/def는 0.95 유지).
- 대신 쉬움 난이도 플레이어에게: ①atk/mag 초기치+레벨업 성장분 모두 20%
  증가(`player.js`/`combat/battle-end.js`), ②모든 스킬 MP 소모량 20% 감소
  (`combat/player-actions.js`의 mpCostMult에 곱연산으로 추가).
- 몬스터 스탯을 거의 건드리지 않아 "몬스터가 약해 보인다"는 문제를 피하면서,
  플레이어가 더 세게 때리고 스킬을 더 자주 쓸 수 있어 체감 난이도가 낮아짐.
- `node --check` 전체 통과. 미검증: 실전 확인 안 함. 기존 세이브는 이미 생성된
  캐릭터의 과거 레벨업분엔 소급 적용 안 되고, 이후 레벨업부터 적용됨.

### 성기사 레벨5 "응징의 맹세" → "응징의 일격" 딜 스킬 전환 (A안)
- 기존: 3턴간 피격 시 40% 확률 반격(리액티브 버프, `counterbuff` 타입).
- 변경: 확정 히트 즉발딜(`phys` 타입, mult1.5) + 잃은 HP 비율만큼 위력 증가
  (최대 +80%, HP가 낮을수록 강해짐) — "고통받을수록 되갚아준다"는 기존
  컨셉을 반격 대신 즉발딜 방식으로 유지.
- 구현: `player-actions.js`의 범용 phys 분기 안에 `key==='retributionoath'`
  특례 처리 추가(다른 특례들과 동일한 패턴). 스킬 id/이름 자리는 그대로
  재사용, `counterbuff` 타입은 이제 참조하는 스킬이 없어 레거시로 남음.
- `node --check` 통과. 미검증: 실전 확인 안 함.

### 밸런스 재조정 — 쉬움 난이도(일반 몬스터가 너무 약해짐)
- 지난 수정치 `{hp:0.9, atk:0.68, def:0.92}`가 일반 몬스터엔 과했다는 피드백.
- `{hp:0.95, atk:0.8, def:0.95}`로 완화(atk 감소폭 32%→20%).

### 진짜 최종보스 전용 후광 추가 — 일반 층별보스보다 크고 강렬하게
- 기존엔 모든 보스(층별보스/최종보스/진짜 최종보스)가 `enemy.isBoss`
  하나로 묶여 똑같은 `.boss-halo`(170px, 은은한 금색)를 공유했음.
- `isTrueFinal`일 때만 별도의 `.true-boss-halo` 클래스를 붙이도록 분리.
  반지름 260px(+바깥 링 340px), 더 쨍한 노랑(#ffe066), 이중 링(코어+테두리)
  구조로 확실히 구분되게 강화. 일반 보스는 기존 `.boss-halo` 그대로 유지.
- `node --check` 통과. 미검증: 브라우저 실전(진짜 최종보스 조우) 확인 안 함.

### 밸런스 수정 — 쉬움 난이도 실질 완화 (목표: 게임 전체 1~2회 사망)
- `getDifficultyMonsterMult()`에서 쉬움이 기존엔 `{hp:1,atk:1,def:1}`로
  사실상 무보정(보통/하드코어 강화의 기준점 그 자체)이었음 — 실질적인
  완화가 전혀 없었음(사용자 제보).
- `{hp:0.9, atk:0.68, def:0.92}`로 변경. 사망의 주원인인 피격 데미지(atk)를
  32% 낮추는 데 집중, hp/def도 소폭 하향.
- 모든 적 생성 경로(일반/정예/보스/최종보스/황금고블린)가 이 함수를 거쳐
  일괄 적용됨.
- `node --check` 전체 통과. 미검증: 실제 목표(1~2회 사망) 달성 여부는
  플레이테스트 필요 — 수치만으로 정확히 보장 불가, 추가 조정 가능성 있음.

### 밸런스 재조정 — 동전 던지기 하향(너무 셌음)
- A안(mult1.4/critMult2.9, 성공시 30딜)이 너무 세다는 피드백 → `mult:1.5,
  critMult:2.0`으로 재조정. 레벨1·노템 기준 성공 시 정확히 22딜(파이어볼과
  동급), 기댓값 11(심판의 일격과 비슷한 수준).

### 밸런스 수정 — 동전 던지기(도박사 Lv1) 상향
- 기존 `mult:1.0, critMult:2.0`(기댓값 1.0배)이 확정 히트인 다른 직업
  1레벨 스킬(1.6~1.9배)보다도 낮았음(사용자 제보). `mult:1.4, critMult:2.9`
  (기댓값 2.03배)로 상향.
- 레벨1·노템·이끼 진 잔가지꾼(방어력2) 기준 시뮬레이션: 기존 기댓값 7 →
  변경 후 15(전사 강타와 비슷), 성공 시 30딜(파이어볼 22보다 위).
- `node --check` 전체 통과. 미검증: 브라우저 실전 확인 안 함.

### 버그 수정 — 전투 시작 토스트가 겹쳐서 안 읽히는 문제
- 원인: `showToast()`가 항상 화면 정중앙 같은 자리에 그려서, 정예 몬스터
  조우처럼 여러 토스트(불확실성의 주사위/조작된 도박판/계율/지속효과/정예
  특성)가 연달아 호출되면 전부 겹쳐서 뒤엣것에 앞엣것이 가려짐.
- 수정: `ui/difficulty.js`의 `showToast()`를 큐 기반으로 변경 — 한 번에
  하나씩, 각 1.8초씩 순서대로 표시. 호출부(battle-setup.js/enemy-turn.js
  등) 코드는 그대로 두고 함수 내부만 수정.
- `node --check` 전체 통과. 미검증: 브라우저 실전 확인 안 함.

### 버그 수정 — 사기꾼 1차 궁극기 실패 시 모든 버튼 비활성화(먹통)
- 원인: 쿨다운 있는 스킬(예: 마지막 카드, `cooldown:3`)은 성공/실패와 무관하게
  **시전하는 순간** 쿨다운이 걸린다. 손버릇(사기꾼)의 무료 재시도 호출이 바로
  이 "쿨다운 중" 방어 코드에 걸려 조용히 `return`되면서, `setCommandsEnabled(true)`도
  `enemyTurn()`도 못 불려 커맨드 버튼이 영구히 비활성화된 채로 남았음.
- 수정: `player-actions.js`의 쿨다운 체크에 `!isRetry` 조건 추가 — 재시도
  호출은 쿨다운 검사를 건너뛴다(MP 체크와 동일한 처리 방식).
- `node --check` 전체 통과. 미검증: 실제 재현(사기꾼 1차 궁극기 실패 유도) 테스트는 못 함.

### 층별보스 보상 강화 — 정예의 인장 1~2개 무작위 추가 지급
- `showBossRewardChoice()`(6지선다 보상 화면) 진입 시, 기존 6개 선택지와는
  별개로 정예의 인장을 1~2개(무작위) 자동으로 더 준다. "정예의 증표" 선택지를
  고르면 거기서 +1이 추가로 더 붙는다(두 지급 경로는 서로 독립).
- 보상 패널 상단에 "🔱 정예의 인장 +N개를 추가로 얻었다!" 안내 문구 추가.
- `node --check` 통과. 미검증: 브라우저 실전 확인 안 함. (참고: 이 보상 화면은
  버튼을 실제로 눌러야 상태가 저장되는 기존 구조라, 버튼 누르기 전 새로고침을
  반복하면 이론상 중복 지급될 수 있음 — 이건 기존 골드/경험치/드랍 전체에
  이미 있던 특성이라 이번 변경으로 새로 생긴 문제는 아님, 별도 이슈로 남겨둠).

### 신규 소모품 "상급 에테르" 추가
- `hiether` 키로 신규: MP 85 회복, 상점가 115G, 보유 상한 2개(상급 물약과
  동일 패턴). 물약/상급물약/에테르와 동일한 비율로 스케일링(에테르 대비
  약 2.75배 회복량, 2.6배 가격).
- 반영 파일: `player.js`(기본 인벤토리), `shop.js`(SHOP_ITEMS/CONSUMABLE_CAPS/
  재고 생성), `combat/player-actions.js`(아이템 사용 효과), `combat/battle-fx.js`
  (전투 중 아이템 목록 UI), `events.js`(포션 지급/기부 이벤트 두 곳),
  `records.js`(빈 자루 유물 발동 조건).
- 저장 데이터는 `player.inv` 객체 전체를 그대로 직렬화하는 구조라 별도
  마이그레이션 불필요(기존 세이브에 hiether 없어도 `||0` 기본값으로 안전).
- `node --check` 전체 통과. 미검증: 브라우저 실전 확인 안 함.

### admin/admin2/admin3 테스트 계정 — 이름 입력 즉시 보통/하드코어 난이도 해금
- `state.js`의 `isAdminNameEntered()`가 이제 admin/admin2/admin3 세 이름을
  모두 인정하도록 확장(기존엔 "admin"만).
- `ui/difficulty.js`의 `isDifficultyUnlocked()`에 이 세 이름이면 보통/
  하드코어도 무조건 해금된 것으로 취급하는 분기 추가(정상 해금 조건인
  기록 유무와 무관).
- 이름 입력칸의 기존 input 이벤트(직업 잠금 실시간 반영용)에 난이도 UI
  재렌더도 같이 걸어, 타이핑하는 즉시(엔터 치기 전에도) 잠금이 풀리는 게
  보이게 함.
- `node --check` 전체 통과. 미검증: 브라우저에서 실제 타이핑 시 즉시 풀리는지 확인 필요.

### 광대 2차 전직 "불운의 채권자" → "사기꾼" 전면 리뉴얼
- "채무 스택 쌓기→청산" 시스템을 폐기하고 사기도박(속임수) 컨셉으로 교체.
  id는 `jester_debtcollector` 재사용(기존 세이브 자동 전환).
- **마스터리 "손버릇"**(mastery_luckdebt 재사용): 운 스킬 실패 시 같은
  스킬이 무료로 1회 자동 재시도. `player-actions.js`에 `checkGamblerRetry()`
  신설, `playerSkill(key, isRetry)`로 시그니처 확장(재시도는 MP 무료).
  운 실패 분기 5곳(goldbet/coinflip/shellgame/gamble/finalcard류) 전부 수정.
- **Lv10 "조작된 도박판"**(자동 발동 패시브): 전투 시작 시 내 무작위 스탯
  (공/마/방/속) +12%, 적 무작위 스탯(공/방/속) -12%. `battle-setup.js`에
  `applyRiggedTable()`/`revertRiggedTableDelta()` 신설(불확실성의 주사위
  `revertDiceDelta()`와 동일한 델타 저장/복구 패턴), 토스트로 결과 안내.
- **Lv12 "속임수 주사위"**: dicecast 타입 굴림을 4/5/6로만 제한.
- **Lv15 "운명 뒤바꾸기"**: 신규 `hpswap` 타입. 성공 시 나와 적의 "남은
  체력 비율"을 서로 맞바꾼다(절대치 아님 — 예: 적 80%/나 10% → 성공 시
  나는 내 최대HP의 80%, 적은 적 최대HP의 10%). 전투당 1회 제한
  (`battleFlags.fateSwapUsed`), `openSub()`에서 1회 소진 시 비활성화 처리.
  실패해도 손버릇으로 1회 무료 재시도 가능.
- 옛 스킬(jesterSettle/jesterReceivable/jesterBankruptcy)은 레거시로 남겨둠.
  이제 안 쓰는 채무 스택 상태 배지(battle-fx.js)는 제거.
- `node --check` 전체 통과. 미검증: 브라우저 실전 테스트 전혀 안 함(손버릇
  재시도 무한루프 여부, HP 스왑 비율 계산, 조작된 도박판 토스트 표시 등).
  수치(12% 등)는 시뮬레이션 없이 사용자 지정값 그대로 반영.

### 방벽 로봇 크기/위치 원복
- 정찰/화력 로봇 크기를 줄이면서(요청 범위) 방벽 로봇 수치도 같이 줄였었는데,
  이건 요청받지 않은 임의 변경이었음(사용자 지적) — 방벽 로봇은 원래 값
  (120x96, top:-8px, ±10px)으로 되돌림. 정찰/화력은 125x100 유지.

### 화력로봇 총구 방향 수정 + 정찰/화력 로봇도 축소·바깥쪽 이동
- 사용자가 직접 재업로드한 화력로봇 이미지의 총구가 반대(바깥쪽)를 향하고
  있어 좌우 반전으로 수정(왼쪽 슬롯 기준 이미지는 총구가 중앙을 향해야 함).
- `.rig-top` 기본 크기를 150x120→125x100으로 줄이고, 왼쪽/오른쪽 슬롯 모두
  기존보다 바깥쪽(-8px)·위쪽(top:-2px)으로 이동 — 정찰/화력 로봇에 적용.
- 방벽 로봇은 그보다 한 단계 더 작고(105x85) 더 바깥쪽(-16px)/위쪽(top:-14px).
- `node --check` 불필요(HTML/이미지만 변경). 미검증: 실제 화면 배치 확인 필요.

### 스킬 목록 UI 재조정 — 각성기 테두리 범위 축소 + admin2 소급지급 버그 수정
- **테두리 색 범위 수정**: 전체 스킬이 아니라 "1차 각성기"(기본 직업
  레벨10 궁극기, 예: 과압각성/메테오/심판의날 등 job.skillLevels[10] 딱 하나)
  와 "2차 각성기"(전직 특성 레벨15 궁극기, spec.skillLevels[15] 딱 하나)
  이 두 스킬에만 테두리 색이 붙도록 `getSkillTier()` 재작성. 나머지 스킬은
  기본 테두리 그대로.
- **admin2/admin3 디버그 계정 버그 수정**: 이 계정들은 전직을 고르기 전에
  레벨업 로직을 17번 먼저 돌려버려서, `specialization.skillLevels`(레벨12/15
  스킬)가 그 시점엔 전부 빗나가 영영 지급되지 않았다(강철 군단장이면
  풀편성 시너지/총사령관의 명령이 안 들어온 채로 남음 — "뒤죽박죽으로 보인다"는
  제보의 실제 원인). `job-advancement.js`의 `resolveJobAdvancement()`에서
  전직 확정 시점에 이미 지난 레벨의 skillLevels를 소급 지급하도록 수정
  (모든 2차 전직에 공통 적용되는 일반 수정 — 강철 군단장 전용 아님).
- `tier2ExtraSkillIds`(jobs.js) 필드는 더 이상 안 쓰여서 제거.
- `node --check` 전체 통과. 미검증: 실제 admin2 재테스트로 재현 확인 필요.

### 스킬 목록 UI 개선 + 강철 군단장 킷 정리
- **1차/2차 각성 스킬 테두리 색 구분**(전 직업 공통): `combat/battle-fx.js`에
  `getSkillTier()` 추가 — 기본 직업(job.skillLevels) 소속이면 1차(테두리
  #5a7a9c), 전직 특성(mastery/active/skillLevels12·15) 소속이면 2차(테두리
  var(--violet))로 스킬 목록·토글 버튼에 클래스 부여. 강철 군단장처럼 1차
  슬롯을 갈아끼운 특수 케이스는 spec에 `tier2ExtraSkillIds`로 별도 지정.
- **강철 군단장 스킬 목록 정렬**: `player.specialization==='mechanic_accumulator'`면
  집중사격명령→취약점분석→전체정비→화력/정찰/방벽 로봇배치→오메가유닛배치→
  풀편성시너지→총사령관의명령 순으로 고정 정렬(사용자 지정 순서).
- **긴급 배치(legionEmergencyDeploy) 완전 제거**: 2차 각성 시 1레벨 자리를
  대체 스킬 없이 그냥 비운다(사용자 요청). `job-advancement.js`의
  `migrateLegionBaseSkills()`에서 swapMap 값을 null로 바꾸고, 이미 지급됐던
  캐릭터를 위한 정리 코드도 추가.
- **방벽 로봇 위치/크기 조정**: 다른 2종(정찰/화력)보다 살짝 작게, 더
  바깥쪽/위쪽으로(`.rig-shield` CSS 클래스 신설).
- `node --check` 전체 통과. 미검증: 실제 화면에서 색/정렬/위치 확인 필요.

### 버그 수정 — 보스전 도중 새로고침 후 이어하기 시 보스 노드 선택 불가
- 원인: `nodemap.js`의 `pickNode()`가 nodeRow/nodeCurrentId를 보스 노드로
  옮기고 `saveGame()`까지 마친 뒤에야 `startBattle()`을 호출한다. 그 사이
  (또는 전투 도중)에 새로고침하면, 지도는 "이미 보스 노드가 현재 위치"인
  채로 저장된다 — 보스 행은 지도의 마지막 행이라 더 이상 다음 행도 없고,
  보스 노드 자신도 "과거(방문 완료)" 취급되어 클릭 불가 — 진행 완전 막힘.
- 수정: `explore.js`의 이어하기(로드) 경로에 복구 분기 추가. `player.nodeRow`가
  지도 마지막 행이고 그 노드가 boss 타입이면(정상적으로 잡았다면 battle-end.js가
  즉시 새 지도로 넘겨버려 이 상태로 저장될 수 없음 — 즉 이 상태 자체가 "전투가
  중간에 끊겼다"는 신호) 보스전을 자동으로 다시 건다. tierIndex 5(최종보스
  분기)면 확인창을 다시 띄운다.
- 새 저장 필드 없이 기존 상태로만 판별(저장 구조 변경 없음). `node --check` 통과.
- 미검증: 실제로 보스전 중 새로고침 재현 테스트는 못 함. 일반 combat/elite
  노드도 이론상 같은 타이밍 문제가 있지만(마지막 행이 아니라 그냥 전투를
  건너뛸 수 있게 되는 정도라 "막힘"은 아님) 이번엔 보스 사례만 수정.

### 로봇 이미지 배경 재수정 — 화력로봇이 흰 배경 그대로 나오던 문제
- 화력로봇 원본이 실제로는 RGB(불투명 흰 배경)였는데, 미리보기의 체크무늬를
  진짜 투명으로 착각하고 배경 제거를 건너뛴 게 원인. 배경 제거 재적용 후
  `images/bot/firepowerbot.png` 교체.

### 로봇 슬롯 위치 수정 — 왼쪽위/오른쪽위로 변경
- `#bt-rig1`/`#bt-rig2`가 예전 "자동포탑"(화면 아래 파묻혀 머리만 보이는 연출,
  `bottom:-100px`) CSS를 그대로 쓰고 있어서 정찰/화력/방벽/긴급배치(필러)
  로봇이 화면 왼쪽아래/오른쪽아래에 표시되던 버그 수정.
- 신규 CSS 클래스 `.rig-top`(화면 위쪽 구석, 150x120px, object-fit:contain)을
  이 4종 kind에만 토글하도록 `battle-fx.js`의 `renderOneRigSlot()`에 추가.
  다른 직업들의 기존 "포탑"류(rigKind:'turret')는 영향 없이 그대로 유지.
- `.rig-turns`(턴 카운터 배지) 위치도 rig-top 전용으로 별도 지정(작은 박스
  기준 하단 안쪽).
- 미검증: 실제 화면에서 겹침/크기 확인 필요.

### 강철 군단장 vs 폭주 화부 상대 밸런스 시뮬레이션 및 조정
- 레벨15 기준(mag≈41) 10턴 시뮬레이션: 강철 군단장(관리형) 894딜/무리스크,
  폭주 화부(원본) 644딜/자해로 최대HP 49% 소모 — 화부가 리스크 대비 딜이
  낮게 나와 화부 쪽 배율을 상향(자해량은 그대로 유지):
  - `mechanicOverloadDischarge.dmgPerPressure` 0.03 → 0.04
  - `mechanicCriticalOverload.dmgPerPressure` 0.04 → 0.06
  - 조정 후: 화부 930딜/자해 동일(최대HP 49% 소모) — 군단장보다 소폭(+4%) 우위,
    리스크 프리미엄 확보.
- 강철 군단장 자체 내부검증(관리형 vs 방치형)도 완료: 894 vs 383딜(2.33배 차),
  "관리 안 하면 손해" 설계 목표 충족 확인.
- `node --check` 전체 통과.

### 기관사 2차 전직 "축압 기술자" → "강철 군단장" 전면 리뉴얼
- 압력 게이지를 완전히 폐기하고, 로봇 3기(정찰/화력/방벽 중 최대 2기 + 오메가
  전용 고정 1자리)를 직접 지휘하는 순수 지속딜형으로 재설계.
- id는 `mechanic_accumulator` 그대로 재사용 — 기존 세이브 캐릭터도 자동 전환.
- 신규: `battleFlags.omegaRig`(오메가 전용 고정 슬롯, rig/rig2 풀과 분리),
  DOM 3번째 슬롯(`#bt-rigomega`, 기존 `.rig-wide` 스타일 재사용).
- 신규 스킬: `legionEmergencyDeploy`(Lv1 긴급배치), `legionMaintenance`(Lv7
  전체정비), `legionFullSquadSynergy`(Lv12 풀편성시너지), `legionCommand`
  (Lv15 총사령관의 명령). 레거시 `mechanicFocusFire`(집중포화)를 Lv3로 재활용.
- 레벨1/3/7 베이스 스킬은 전직 이전에 이미 지급되는 구조라, 레벨업 시점이
  아니라 `job-advancement.js`의 `migrateLegionBaseSkills()`에서 전직 시점 +
  매 전투 시작 시점(기존 세이브 마이그레이션용) 양쪽에서 스왑 처리.
- 로봇 그림 3종(정찰/화력/방벽) 사용자 제공 이미지로 교체(`images/bot/`),
  왼쪽 슬롯 기준으로 배경 투명화 + 좌우반전 + 일부 회전 처리해 저장.
- 변경 파일: `data/jobs.js`, `data/skills.js`, `combat/player-actions.js`,
  `combat/enemy-turn.js`, `combat/battle-fx.js`, `combat/job-advancement.js`,
  `combat/battle-setup.js`, `monster-visuals.js`, `index.html`.
- `node --check` 전체 통과.

## 최근 검증 상태

- 문법 검사(`node --check`)만 완료, **브라우저 실전 테스트는 아직 못 함**.
- 미검증: 강철 군단장 신규 스킬 6종(긴급배치/전체정비/풀편성시너지/총사령관의
  명령/오메가배치/집중사격명령) 수치 — 시뮬레이션 내부 검증은 마쳤으나 실전
  플레이 감각은 미확인.
- 미검증: 기존 세이브(이미 축압 기술자로 전직한 캐릭터) 마이그레이션 실제 동작.
- 미검증: 오메가 전용 슬롯 UI(왼쪽/오른쪽 슬롯과 겹치지 않는지) 실제 화면 확인.
- 미검증: 폭주 화부 상향 수치가 다른 2차 전직들과 비교해서도 적절한지(이번엔
  강철 군단장과의 상대 비교만 진행, 전체 클래스 간 비교는 안 함).

## 현재 작업

- 현재 작업은 새 기능 요청이 들어오면 이 문서를 업데이트한다.
- 작업 완료 후 "현재 작업", "완료", "미검증", "다음 작업"을 갱신한다.

## 완료된 것으로 취급할 수 있는 영역

프로젝트에는 다음 핵심 시스템이 이미 존재한다.

- 기본 직업 시스템
- 하이브리드 2차 전직
- 턴제 1:1 전투
- 몬스터/보스
- 스킬
- 장비
- 유물
- 탐험
- 상점
- 저장/불러오기
- 기록/도감
- Web Audio 기반 사운드
- 몬스터 시각화
- 이벤트/노드/기원 관련 코드

단, "완전히 테스트 완료"라는 의미는 아니다. 실제 테스트 여부는 해당 작업의 검증 결과를 따른다.

## 작업 시 주의

### 전투
전투 파일들은 서로 강하게 연결되어 있다. 작은 변경도 호출 순서와 턴 전환에 영향을 줄 수 있다.

### 저장
저장 필드 변경은 `player.js`와 `storage.js`를 함께 확인한다.

### 직업
직업/전직 변경은 `jobs.js`, `job-advancement.js`, 필요 시 `skills.js`, `battle-end.js`를 함께 확인한다.

### 스킬
스킬 데이터와 실행 로직이 분리되어 있으므로 `data/skills.js`만 수정하고 끝내지 않는다.

### 몬스터
데이터/전투 생성/시각화가 분리되어 있으므로 작업 성격에 맞는 파일만 선택한다.

## 다음 작업

```text
### 강철 군단장 밸런스 시뮬레이션 및 실전 검증
- 목표: 신규/재배치된 스킬 6개(긴급배치/집중사격명령/전체정비/오메가배치/
  풀편성시너지/총사령관의명령)의 mp·배율·지속시간 수치를 Python Monte Carlo로 조정
- 수정 예정 파일: data/skills.js (수치만), 필요 시 player-actions.js/enemy-turn.js
- 완료 조건: 다른 2차 전직들과 비슷한 페이스로 클리어 가능한 수치 확정
- 현재 문제: 모든 수치가 임시값 상태
- 검증 방법: 브라우저 실전 플레이 + 시뮬레이션 결과 비교

### 브라우저 실전 테스트
- 목표: 오메가 슬롯 UI 위치/겹침, 로봇 3종 이미지 표시, 레벨1/3/7 스킬 교체가
  실제로 정상 동작하는지 확인
- 완료 조건: 실제 브라우저에서 전직→전투 1회 이상 플레이해 크래시 없음 확인
```

## 상태 업데이트 규칙

작업이 끝나면:

1. 완료된 작업을 추가한다.
2. 변경 파일을 기록한다.
3. 미검증 항목을 기록한다.
4. 다음 작업을 1~5개 이내로 적는다.
5. 오래된 완료 항목은 필요하면 별도 HANDOFF/CHANGELOG로 이동한다.

이 문서는 길게 만들지 않는다. 현재 상태만 유지한다.
